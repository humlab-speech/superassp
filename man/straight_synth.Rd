% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_python_straight_synth.R
\name{straight_synth}
\alias{straight_synth}
\title{STRAIGHT Speech Synthesis (Legacy Vocoder)}
\usage{
straight_synth(
  f0,
  spec,
  ap = NULL,
  sample_rate = 22050,
  frame_shift = 1,
  output_file = NULL,
  verbose = TRUE
)
}
\arguments{
\item{f0}{Numeric vector; F0 contour in Hz}

\item{spec}{Numeric matrix; spectral envelope \link{frames x frequencies}}

\item{ap}{Numeric matrix (optional); aperiodicity \link{frames x frequencies}}

\item{sample_rate}{Numeric; sampling rate in Hz (default: 22050)}

\item{frame_shift}{Numeric; frame shift in milliseconds (default: 1.0)}

\item{output_file}{Character (optional); path to save synthesized audio}

\item{verbose}{Logical; print progress messages (default: TRUE)}
}
\value{
Numeric vector of synthesized audio samples (range: \link{-1, 1})
}
\description{
Synthesizes speech from STRAIGHT parameters (F0, spectral envelope, aperiodicity).
Provides high-quality speech resynthesis for vocoding and voice conversion.
}
\details{
The STRAIGHT synthesis algorithm reconstructs speech from vocoder parameters
using source-filter decomposition with high-quality pitch-adaptive synthesis.

\strong{Input Requirements}:
\itemize{
\item F0: Fundamental frequency contour (use \code{trk_straight_f0()})
\item Spectral envelope: Pitch-adaptive smoothed spectrum (use \code{trk_straight_spec()})
\item Aperiodicity (optional): Noise component (default: all periodic)
}

\strong{Quality}: Perceptually identical to MATLAB STRAIGHT synthesis
}
\examples{
\dontrun{
# Check availability
if (!straight_available()) {
  install_legacy_straight()
}

# Full analysis-synthesis cycle
wav_file <- system.file("samples", "sustained", "a1.wav", package = "superassp")

# Extract parameters
f0_data <- trk_straight_f0(wav_file, toFile = FALSE)
spec_data <- trk_straight_spec(wav_file, toFile = FALSE)

# Synthesize
audio_synth <- straight_synth(
  f0 = f0_data$f0[,1],
  spec = spec_data$spec,
  sample_rate = 22050,
  output_file = "resynthesized.wav"
)

# Play synthesized audio
av::av_audio_convert(audio_synth, "resynthesized.wav",
                     sample_rate = 22050, channels = 1)
}

}
\references{
\insertRef{Kawahara.1999.SpeechCommunication}{superassp}
}
\seealso{
\code{\link{trk_straight_f0}}, \code{\link{trk_straight_spec}},
\code{\link{install_legacy_straight}}
}
