% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_helpers.R
\name{.has_placeholder}
\alias{.has_placeholder}
\title{Detect if track name has placeholder 'i'}
\usage{
.has_placeholder(name)
}
\arguments{
\item{name}{Character. Track name template to check.}
}
\value{
Logical. TRUE if name contains placeholder, FALSE otherwise.
}
\description{
Checks if a track name template contains the placeholder 'i' using the
uniform pattern: uppercase letter + 'i' + (bracket or end-of-string).
}
\details{
The pattern \verb{[A-Z]i(\\\[|$)} matches:
\itemize{
\item Fi[Hz] → TRUE (formant frequency template)
\item Bi[Hz] → TRUE (bandwidth template)
\item LPCi → TRUE (LP coefficient template)
\item ARFi → TRUE (ARF coefficient template)
\item Hi[dB] → TRUE (harmonic template)
\item Ai[dB] → TRUE (amplitude template)
}

But excludes:
\itemize{
\item foi[Hz] → FALSE (lowercase before i)
\item intensity → FALSE (lowercase, i not at end)
\item pitch[Hz] → FALSE (no i before bracket)
\item gain[dB] → FALSE (no i)
}
}
\examples{
\dontrun{
.has_placeholder("Fi[Hz]")     # TRUE
.has_placeholder("LPCi")       # TRUE
.has_placeholder("fo[Hz]")     # FALSE
.has_placeholder("intensity")  # FALSE
}
}
\keyword{internal}
