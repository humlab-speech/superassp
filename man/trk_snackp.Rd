% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_python_snack_pitch.R
\name{trk_snackp}
\alias{trk_snackp}
\title{Snack-style Pitch Tracking}
\usage{
trk_snackp(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Vector of file paths to audio files}

\item{beginTime}{Start time in seconds (default: 0.0)}

\item{endTime}{End time in seconds (default: 0.0 = end of file)}

\item{windowShift}{Frame shift in milliseconds (default: 10.0)}

\item{windowLength}{Analysis window length in milliseconds (default: 7.5)}

\item{minF}{Minimum F0 in Hz (default: 50)}

\item{maxF}{Maximum F0 in Hz (default: 550)}

\item{threshold}{Correlation threshold for peak detection (default: 0.3)}

\item{toFile}{Write results to file (default: TRUE)}

\item{explicitExt}{File extension for output files (default: "snackpitch")}

\item{outputDirectory}{Directory for output files (default: NULL = same as input)}

\item{verbose}{Print progress messages (default: TRUE)}
}
\value{
If toFile=FALSE, returns AsspDataObj or list of AsspDataObjs with tracks:
\itemize{
\item f0: fundamental frequency in Hz (0 = unvoiced)
\item voicing: voicing probability (0-1)
\item rms: RMS energy per frame
}
If toFile=TRUE, returns number of files successfully processed.
}
\description{
Extracts fundamental frequency (F0) using the Snack pitch tracker algorithm with
in-memory audio processing. Audio is loaded using the av package, eliminating
temporary file creation. Supports all media formats (WAV, MP3, MP4, etc.).
This implementation replicates the pitch tracking method from the Snack Sound
Toolkit, which uses autocorrelation with dynamic programming for robust F0 estimation.
}
\details{
The Snack pitch tracker uses normalized autocorrelation to find pitch candidates
at each frame, then applies dynamic programming to select the most likely F0
trajectory. This approach balances local pitch estimates with trajectory smoothness.

Default parameters match Snack's defaults:
\itemize{
\item minF = 50 Hz, maxF = 550 Hz
\item windowShift = 10 ms
\item windowLength = 7.5 ms
\item threshold = 0.3 (correlation threshold)
}

This implementation provides compatibility with analyses using Snack as a reference.
}
\examples{
\dontrun{
# Basic usage
trk_snackp("audio.wav")

# Return data without file
pitch_data <- trk_snackp("audio.wav", toFile = FALSE)
plot(pitch_data$f0, type = "l")

# Custom parameters
trk_snackp("speech.wav", minF = 75, maxF = 400, windowShift = 5)

# Batch processing
files <- list.files("audio_dir", pattern = ".wav$", full.names = TRUE)
trk_snackp(files, verbose = TRUE)
}

}
\references{
\insertRef{Sjolander2000}{superassp}
}
\seealso{
\code{\link{rapt}}, \code{\link{swipe}}, \code{\link{kaldi_pitch}}
for alternative pitch tracking methods
}
