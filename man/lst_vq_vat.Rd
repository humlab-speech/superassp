% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_voiceanalysis_vq.R
\name{lst_vq_vat}
\alias{lst_vq_vat}
\title{Per-GCI voice-quality summary via voiceanalysis}
\usage{
lst_vq_vat(
  listOfFiles,
  beginTime = 0,
  endTime = 0,
  toFile = FALSE,
  return_jstf = FALSE,
  explicitExt = "vqv",
  outputDirectory = NULL,
  verbose = TRUE
)
}
\arguments{
\item{listOfFiles}{Character vector of audio file paths.}

\item{beginTime, endTime}{Analysis window (seconds). Defaults: full file.}

\item{toFile}{Logical. If TRUE, persist results as JSTF and return paths.
Default FALSE.}

\item{return_jstf}{Logical. Return JsonTrackObj instead of data.frame? Default FALSE. When both toFile and return_jstf are TRUE, the file is written AND the object returned.}

\item{explicitExt}{File extension. Default "vqv".}

\item{outputDirectory}{Output directory. Default NULL = next to input.}

\item{verbose}{Logical. Default TRUE.}
}
\value{
If \code{toFile = FALSE} and \code{length(listOfFiles) == 1}:
a named list with vectors \code{gci_time} (seconds),
\code{NAQ}, \code{QOQ}, \code{H1H2}, \code{HRF}. For multiple files,
a list of such lists. If \code{toFile = TRUE}: invisible vector of
output paths.
}
\description{
Extracts the canonical voice-source parameters
\insertCite{KaneGobl2013}{superassp} from an audio file using
\pkg{voiceanalysis}: NAQ (Normalised Amplitude Quotient), QOQ
(Quasi-Open Quotient), H1H2 spectral tilt, and HRF (Harmonic Richness
Factor). Internally runs SE-VQ for GCIs, IAIF for the glottal flow
derivative, then \code{vat_voice_quality()}.
}
\details{
Returns one row per GCI. To get continuous-track equivalents, interp
to a 10 ms grid via \code{stats::approx}.
}
\references{
\insertCite{KaneGobl2013}{superassp}
}
\seealso{
\code{\link{lst_covarep_vq}}
}
