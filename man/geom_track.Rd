% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggtrack_geoms.R
\name{geom_track}
\alias{geom_track}
\title{Plot time-aligned tracks of an AsspDataObj}
\usage{
geom_track(
  mapping = NULL,
  data = NULL,
  ...,
  tracks = NULL,
  time = "frame_time",
  na.zeros = FALSE,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = FALSE,
  stat = "identity",
  position = "identity"
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}},
applied to the track table the layer prepares (\code{frame_time}, \code{value},
\code{track}, \code{band}, \code{bin}). When \code{NULL} (default) the tracks are drawn with
\code{aes(x = frame_time, y = value, colour = track, group = track)}.}

\item{data}{The data to display: an \code{AsspDataObj} (from a \verb{trk_*} function,
\code{\link[=read_audio]{read_audio()}}, \code{\link[=read_ssff]{read_ssff()}} or \code{\link[=read_track]{read_track()}}), the wide table from
\code{\link[=as.data.frame.AsspDataObj]{as.data.frame.AsspDataObj()}}, or an already long track table with
\code{frame_time}, \code{value} and \code{track} columns. Inherited from the plot when
\code{NULL}.}

\item{...}{Other arguments passed on to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{tracks}{Character. Tracks to plot, matched against the column name
(\code{"RMS_dB"}), the track template (\code{"RMS[dB]"}) or the object's track name.
\code{NULL} (default) plots every track.}

\item{time}{Character. Preferred name of the time column of \code{data}. The
plotted table always names it \code{frame_time}.}

\item{na.zeros}{Logical. Convert stored zeros to \code{NA}, so that undefined
frames (unvoiced f0, absent formants) break the line instead of dropping to
zero. Default: \code{FALSE}.}

\item{na.rm}{Logical. Remove \code{NA} values before drawing. Default: \code{FALSE}.}

\item{show.legend, inherit.aes, stat, position}{Passed on to
\code{\link[ggplot2:layer]{ggplot2::layer()}}, with ggplot2's usual defaults except
\code{inherit.aes = FALSE} (see Details).}
}
\value{
A ggplot2 layer.
}
\description{
A ggplot2 layer that draws the tracks of an \code{AsspDataObj} (or of the wide
table written by \code{\link[=as.data.frame.AsspDataObj]{as.data.frame.AsspDataObj()}}) as one line per track,
against time in seconds. Scales, facets, coordinates, legends and themes
apply as with any other layer.
}
\details{
Track data is rewritten into a long table (\code{frame_time}, \code{value}, \code{track},
\code{band}, \code{bin}) before the layer is built, because ggplot2 hands a layer only
its evaluated aesthetics. A \code{mapping} therefore refers to that table, and
\code{inherit.aes} defaults to \code{FALSE}: the mapping goes on the layer, as in
\code{geom_track(data = obj, mapping = aes(x = frame_time, y = value, group = track))}.
\code{tracks} also matches bands, so \code{tracks = "Fi[Hz]"} draws F1..Fn while
\code{tracks = "F1_Hz"} draws a single formant.
For a column-wise plot of the wide table (one column against another), use
\code{\link[ggplot2:geom_line]{ggplot2::geom_line()}} or \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}} on
\code{\link[=as.data.frame.AsspDataObj]{as.data.frame.AsspDataObj()}} instead: \code{ggtrack(df, aes(x = F2_Hz, y = F1_Hz)) + geom_point()}.
Layers on different objects compose, so a waveform can be drawn over a
spectrogram by adding one \code{\link[=geom_spectrogram]{geom_spectrogram()}} and one \code{geom_track()}.
The layer carries one row per record and track (a waveform has one row per
sample), so window a long recording at read time with
\verb{read_audio(wav, begin = …, end = …)}.
}
\examples{
wav <- system.file("samples", "sustained", "a1.wav", package = "superassp")
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  library(ggplot2)

  # every track of the object, one line each
  rms <- trk_rms(wav, toFile = FALSE, verbose = FALSE)
  ggplot() + geom_track(data = rms)

  # a subset, labelled from the track metadata
  fms <- trk_formant_forest(wav, numFormants = 3, toFile = FALSE, verbose = FALSE)
  ggtrack(fms) + geom_track(tracks = c("F1_Hz", "F2_Hz", "F3_Hz"))

  # the raw waveform
  audio <- read_audio(wav, samples = TRUE)
  ggplot() + geom_track(data = audio)

  # a waveform drawn over the spectrogram of the same file
  dft <- trk_dft_spectrum(wav, toFile = FALSE, verbose = FALSE)
  ggplot() +
    geom_spectrogram(data = dft) +
    geom_track(data = audio,
               mapping = aes(x = frame_time, y = value / 8000 + 3000),
               colour = "white", linewidth = 0.2) +
    coord_cartesian(ylim = c(0, 6000))
}
}
}
\seealso{
\code{\link[=geom_spectrogram]{geom_spectrogram()}} for multi-column spectral tracks, \code{\link[=ggtrack]{ggtrack()}}
for automatic track labels, \code{\link[=as.data.frame.AsspDataObj]{as.data.frame.AsspDataObj()}} for the wide
table.
}
