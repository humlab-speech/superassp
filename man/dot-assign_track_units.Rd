% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_helpers.R
\name{.assign_track_units}
\alias{.assign_track_units}
\title{Assign units to data frame columns}
\usage{
.assign_track_units(df)
}
\arguments{
\item{df}{Data frame. Data frame with cleaned column names.}
}
\value{
Data frame with units assigned to appropriate columns.
}
\description{
Assigns R units package units to columns based on their unit suffix.
}
\details{
Maps common unit suffixes to R units package units:
\itemize{
\item \verb{_Hz} → Hz
\item \verb{_dB} → dB (dimensionless, but labeled)
\item \verb{_pct} → percent (dimensionless)
\item \verb{_us} → microseconds
\item \verb{_ms} → milliseconds
\item \verb{_s} → seconds
\item \verb{_Bark} → Bark (dimensionless)
\item \verb{_mel} → mel (dimensionless)
\item \verb{_ERB} → ERB (dimensionless)
}

Requires the 'units' package to be installed.
}
\examples{
\dontrun{
df <- data.frame(frame_time = 1:10, fo_Hz = rnorm(10, 120, 10))
df <- .assign_track_units(df)
class(df$fo_Hz)  # "units"
}
}
\keyword{internal}
