% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_cpp_crepe.R
\name{trk_pitch_crepe}
\alias{trk_pitch_crepe}
\title{Track fundamental frequency and periodicity using CREPE (ONNX)}
\usage{
trk_pitch_crepe(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Character vector of audio file paths. Any format
supported by \pkg{av} is accepted.}

\item{beginTime}{Numeric. Start of analysis window in seconds. Default 0
(file start).}

\item{endTime}{Numeric. End of analysis window in seconds. Default 0
(file end).}

\item{windowShift}{Numeric. Frame shift in milliseconds; sets output frame
rate (\code{1000 / windowShift} Hz). Default 10 ms (100 Hz).}

\item{windowSize}{Numeric. Smoothing filter window size in milliseconds,
applied to both median (periodicity) and mean (F0) post-processing filters.
Default 15 ms.}

\item{minF}{Numeric. Minimum F0 in Hz. Frames with estimated F0 below this
value are set to 0 (unvoiced). Default 50 Hz.}

\item{maxF}{Numeric. Maximum F0 in Hz. Frames above this value are set to 0.
Default 550 Hz.}

\item{voicing.threshold}{Numeric. Periodicity threshold for voicing
decisions. Frames with periodicity below this value are set to unvoiced
(F0 = 0). Default 0.21.}

\item{silence.threshold}{Numeric. A-weighted dB threshold relative to global
maximum. Frames below this are treated as silent (periodicity = 0).
Default -60 dB.}

\item{model}{Character. Model variant: \code{"tiny"} (fast) or
\code{"full"} (more accurate). Default \code{"tiny"}.}

\item{decoder}{Character. Decoding method: \code{"viterbi"} (recommended,
smoother) or \code{"argmax"} (faster but noisier). Default
\code{"viterbi"}.}

\item{batch_size}{Integer. Number of frames per ONNX inference batch.
Default 512.}

\item{explicitExt}{Character. Output file extension. Default \code{"crp"}.}

\item{outputDirectory}{Character. Directory for output files. \code{NULL}
(default) writes alongside the input file.}

\item{toFile}{Logical. If \code{TRUE}, write SSFF output files and return
the count written (invisibly). If \code{FALSE}, return an
\code{AsspDataObj}. Default \code{TRUE}.}

\item{verbose}{Logical. Print per-file progress. Default \code{TRUE}.}
}
\value{
If \code{toFile = FALSE}: an \code{AsspDataObj} with tracks:
\describe{
\item{\code{f0}}{REAL32, Hz, \emph{n\_frames} × 1. Fundamental
frequency; 0 in unvoiced/silent frames.}
\item{\code{periodicity}}{REAL32, 0–1, \emph{n\_frames} × 1. Model
confidence; values below \code{voicing.threshold} are treated as
unvoiced.}
}
Frame rate: \code{1000 / windowShift} Hz (default 100 Hz, 10 ms hop).
If \code{toFile = TRUE}: integer count of files written, returned invisibly.
}
\description{
Estimates F0 by applying a deep convolutional neural network directly to the
time-domain waveform
(CREPE; \insertCite{Kim.2018.10.1109/icassp.2018.8461329}{superassp}). Two
model sizes are available: \code{"tiny"} (~1.9 MB, fast) and \code{"full"}
(~85 MB, more accurate). No Python or PyTorch required — inference uses the
bundled ONNX Runtime.
}
\details{
ONNX Runtime is installed automatically on first use (~30 MB, cached in
the R user directory) and persists across R sessions and package reinstalls.

Post-processing (matching torchcrepe): median filter on periodicity →
A-weighted silence detection → voicing threshold → NaN-aware mean filter
on F0 → F0 range clamping.
}
\references{
\insertAllCited{}
}
