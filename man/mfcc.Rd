% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_python_torch_mfcc.R
\name{mfcc}
\alias{mfcc}
\title{Extract MFCC features using torchaudio (DEPRECATED)}
\usage{
mfcc(
  listOfFiles = NULL,
  beginTime = 0,
  endTime = 0,
  windowShift = 10,
  windowSize = 25,
  n_mfcc = 13,
  n_mels = 40,
  fmin = 0,
  fmax = NULL,
  toFile = TRUE,
  explicitExt = "mfcc",
  outputDirectory = NULL,
  verbose = TRUE
)
}
\arguments{
\item{listOfFiles}{Vector of file paths to process}

\item{beginTime}{Start time in seconds (default: 0.0)}

\item{endTime}{End time in seconds (default: 0.0 = end of file)}

\item{windowShift}{Frame shift in milliseconds (default: 10.0)}

\item{windowSize}{FFT window size in milliseconds (default: 25.0)}

\item{n_mfcc}{Number of MFCC coefficients to return (default: 13)}

\item{n_mels}{Number of mel filterbanks (default: 40)}

\item{fmin}{Minimum frequency in Hz (default: 0.0)}

\item{fmax}{Maximum frequency in Hz (default: NULL = sample_rate/2)}

\item{toFile}{Write results to file (default: TRUE)}

\item{explicitExt}{Output file extension (default: "mfcc")}

\item{outputDirectory}{Output directory (default: NULL = same as input)}

\item{verbose}{Show progress messages (default: TRUE)}
}
\value{
If toFile=TRUE, returns the number of successfully processed files.
If toFile=FALSE, returns AsspDataObj with MFCC tracks (mfcc_1, mfcc_2, ..., mfcc_n).
}
\description{
\strong{DEPRECATED}: Please use \code{\link[=trk_mfcc]{trk_mfcc()}} instead, which provides a faster
C++-based implementation using the SPTK library (2-3x faster) without
requiring Python dependencies.

Compute Mel-frequency cepstral coefficients (MFCCs) from audio using torchaudio.
MFCCs are widely used features in speech recognition, speaker identification,
and other audio analysis tasks.

The implementation follows the standard MFCC pipeline:
\enumerate{
\item Compute power spectrogram via STFT
\item Apply mel-scale filterbank
\item Convert to dB scale
\item Apply Discrete Cosine Transform (DCT)
}

All input media formats are supported via torchaudio, including audio extraction
from video files.
}
\note{
\strong{DEPRECATED}: This function requires torch and torchaudio.
Use \code{\link[=trk_mfcc]{trk_mfcc()}} for better performance.
}
\examples{
\dontrun{
# Extract 13 MFCCs (default)
mfcc("recording.wav")

# Extract 20 MFCCs with custom parameters
mfcc("speech.mp3", n_mfcc = 20, n_mels = 80)

# Return data without writing file
mfcc_data <- mfcc("audio.wav", toFile = FALSE)

# Process with specific frequency range
mfcc("recording.wav", fmin = 80, fmax = 8000)
}
}
\seealso{
\code{\link[=trk_mfcc]{trk_mfcc()}} for the recommended C++ implementation
}
