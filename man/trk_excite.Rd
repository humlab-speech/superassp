% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_python_excite.R
\name{trk_excite}
\alias{trk_excite}
\title{Extract excitation signal from pitch}
\usage{
trk_excite(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Vector of file paths to process}

\item{voicing.threshold}{Voicing threshold for SWIPE pitch detection (default: 0.3)}

\item{use.gaussian}{Use Gaussian noise for unvoiced frames (default: FALSE)}

\item{interpolation.period}{Interpolation period (default: 1)}

\item{gaussian.seed}{Seed for Gaussian random number generator (default: 1)}

\item{conda.env}{Optional conda environment name}
}
\value{
An SSFF track object containing the excitation signal that is either
returned (toFile == FALSE) or stored on disk.
}
\description{
Computes the excitation signal from pitch using the SPTK excite function.
The excitation signal represents the source signal in source-filter decomposition.
}
\details{
This function uses the pysptk Python library for excitation extraction. Audio files
are loaded via the \code{av} package, supporting all media formats (WAV, MP3, MP4, video files, etc.).
}
