% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_helpers.R
\name{.clean_track_names}
\alias{.clean_track_names}
\title{Clean track names for R data frames}
\usage{
.clean_track_names(names)
}
\arguments{
\item{names}{Character vector. Track names to clean.}
}
\value{
Character vector of cleaned names.
}
\description{
Converts SSFF-style track names (with brackets and hyphens) to R-friendly
column names (with underscores).
}
\details{
Applies the following transformations:
\enumerate{
\item \verb{[Hz]} → \verb{_Hz} (brackets to underscores)
\item \code{H1-H2} → \code{H1_H2} (hyphens to underscores)
\item \code{(local)} → \verb{_local} (parentheses to underscores)
\item Multiple spaces → single underscore
}

This follows the hybrid naming strategy where SSFF files use scientific
notation with brackets, but R data frames use clean underscore notation.
}
\examples{
\dontrun{
.clean_track_names(c("fo[Hz]", "F1[Hz]", "H1-H2c[dB]"))
# [1] "fo_Hz" "F1_Hz" "H1_H2c_dB"

.clean_track_names(c("Jitter (local)[\%]", "Shimmer (dda)[\%]"))
# [1] "Jitter_local_pct" "Shimmer_dda_pct"
}
}
\keyword{internal}
