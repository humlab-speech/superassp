% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_cpp_sptk_mfcc.R
\name{sptk_mfcc}
\alias{sptk_mfcc}
\title{SPTK MFCC Extraction (C++ implementation)}
\usage{
sptk_mfcc(
  listOfFiles = NULL,
  beginTime = 0,
  endTime = 0,
  windowShift = 10,
  windowSize = 25,
  n_mfcc = 13,
  n_mels = 40,
  fmin = 0,
  fmax = NULL,
  lifter = 22,
  floor = 1,
  toFile = TRUE,
  explicitExt = "mfcc",
  outputDirectory = NULL,
  verbose = TRUE
)
}
\arguments{
\item{listOfFiles}{Vector of file paths to process}

\item{beginTime}{Start time in seconds (default: 0.0)}

\item{endTime}{End time in seconds (default: 0.0 = end of file)}

\item{windowShift}{Frame shift in milliseconds (default: 10.0)}

\item{windowSize}{Window size in milliseconds (default: 25.0)}

\item{n_mfcc}{Number of MFCC coefficients to extract (default: 13)}

\item{n_mels}{Number of mel filterbanks (default: 40)}

\item{fmin}{Minimum frequency in Hz (default: 0.0)}

\item{fmax}{Maximum frequency in Hz (default: NULL = sample_rate/2)}

\item{lifter}{Liftering coefficient (default: 22)}

\item{floor}{Floor value for mel filterbank output (default: 1.0)}

\item{toFile}{Write results to file (default: TRUE)}

\item{explicitExt}{Output file extension (default: "mfcc")}

\item{outputDirectory}{Output directory (default: NULL = same as input)}

\item{verbose}{Show progress messages (default: TRUE)}
}
\value{
If toFile=TRUE, returns the number of successfully processed files.
If toFile=FALSE, returns AsspDataObj or list of AsspDataObj objects
with MFCC tracks (mfcc_0, mfcc_1, ..., mfcc_n).
}
\description{
Extract Mel-Frequency Cepstral Coefficients (MFCCs) using the
SPTK library. This is a high-performance C++ implementation that is
significantly faster than Python-based implementations and requires
no Python dependencies.

MFCCs are widely used acoustic features in speech recognition, speaker
identification, and audio analysis. The implementation follows the
standard HTK-style MFCC pipeline with mel-scale filterbanks and
discrete cosine transform.

All input media formats are supported via the av package, including video
files from which audio will be automatically extracted.
}
\note{
This function uses the SPTK C++ library for high-performance MFCC
extraction. It is 2-3x faster than the Python torchaudio implementation.
}
\examples{
\dontrun{
# Extract 13 MFCCs (default)
sptk_mfcc("recording.wav")

# Extract 20 MFCCs with custom parameters
sptk_mfcc("speech.mp3", n_mfcc = 20, n_mels = 80)

# Return data without writing file
mfcc_data <- sptk_mfcc("audio.wav", toFile = FALSE)

# Process with specific frequency range
sptk_mfcc("recording.wav", fmin = 80, fmax = 8000)

# Process video file (extracts audio)
sptk_mfcc("interview.mp4")
}
}
\seealso{
\code{\link[=mfcc]{mfcc()}} for the deprecated torchaudio implementation
}
