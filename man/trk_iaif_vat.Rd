% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_voiceanalysis_iaif.R
\name{trk_iaif_vat}
\alias{trk_iaif_vat}
\title{Estimate glottal flow via IAIF using voiceanalysis}
\usage{
trk_iaif_vat(
  listOfFiles,
  beginTime = 0,
  endTime = 0,
  p = NULL,
  toFile = TRUE,
  explicitExt = "glv",
  outputDirectory = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{listOfFiles}{Character vector of audio file paths. Any format supported by
\pkg{av} is accepted; non-native inputs are transcoded automatically.}

\item{...}{Additional arguments (currently unused).}

\item{p}{LPC prediction order. \code{NULL} (default) sets \code{round(fs/1000)+2}.}

\item{beginTime}{Start time for the extracted portion in seconds. Default: NULL (beginning of signal). Note: uses \code{beginTime}/\code{endTime} (seconds) matching DSP function conventions, unlike \code{\link[=read_audio]{read_audio()}} which uses \code{begin}/\code{end}.}

\item{endTime}{The end time of the section of the sound files that should be analysed (in seconds). Use 0 for end of file.}

\item{toFile}{Logical. If \code{TRUE}, write SSFF output files and return the count written. If \code{FALSE}, return an \code{AsspDataObj} (single file only). Default \code{TRUE}.}

\item{explicitExt}{By default, a character "d" will be prepended to the file name suffix when writing the output to file. The user can also specify an explicit extension which will be used instead.}

\item{outputDirectory}{The directory where the slice file should be stored. If not defiled (NULL), the sparse slice file will placed in the same folder as the media file.}

\item{verbose}{Logical. Show a progress bar (sequential path) or a progress-aware parallel apply (\code{pbapply}/\code{pbmcapply}, if installed).}
}
\value{
If \code{toFile = FALSE}: an \code{AsspDataObj} with tracks:
\describe{
\item{\code{glottal_flow}}{REAL64, integrated glottal flow,
n_samples × 1.}
\item{\code{glottal_derivative}}{REAL64, glottal flow derivative,
n_samples × 1.}
}
Frame rate equals the audio sample rate. Schema matches
\code{trk_covarep_iaif()} for swap-in convenience.
If \code{toFile = TRUE}: invisibly returns the count of files written.
}
\description{
Iterative Adaptive Inverse Filtering \insertCite{Alku1992}{superassp},
driven by the bit-faithful Rcpp port in the \pkg{voiceanalysis} package.
GCIs are detected internally using SE-VQ.
}
\details{
Alternative to \code{\link{trk_covarep_iaif}}, which uses superassp's
own native C++ IAIF implementation. The two implementations differ in
parameterisation (\code{trk_iaif_vat} uses the MATLAB-VAT defaults and
the SE-VQ GCI grid; \code{trk_covarep_iaif} uses COVAREP defaults and a
contiguous frame grid).
}
\examples{
\dontrun{
trk_iaif_vat(
  system.file("samples", "sustained", "a1.wav", package = "superassp"),
  toFile = FALSE
)
}
}
\references{
\insertCite{Alku1992}{superassp}
\insertCite{KaneGobl2013}{superassp}
}
\seealso{
\code{\link{trk_covarep_iaif}}, \code{\link{trk_gci_vat}}
}
