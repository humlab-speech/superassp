% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_python_dv_f0.R
\name{trk_dv_f0}
\alias{trk_dv_f0}
\title{Track Fundamental Frequency (F0) using DisVoice}
\usage{
trk_dv_f0(
  audio_path,
  frame_shift = 10,
  min_f0 = 75,
  max_f0 = 600,
  include_voicing = TRUE,
  toFile = FALSE,
  explicitExt = "dvf",
  outputDirectory = NULL,
  output_format = c("AsspDataObj", "dataframe", "list"),
  ...
)
}
\arguments{
\item{audio_path}{Character string: path to audio file (any format supported by av)}

\item{frame_shift}{Numeric: frame shift in milliseconds (default: 10ms = 100 Hz)}

\item{min_f0}{Numeric: minimum F0 in Hz (default: 75)}

\item{max_f0}{Numeric: maximum F0 in Hz (default: 600)}

\item{include_voicing}{Logical: include voicing decision track (default: TRUE)}

\item{toFile}{Logical: if TRUE, write result to SSFF file (default: FALSE)}

\item{explicitExt}{Character: file extension for output SSFF file (default: "dvf")}

\item{outputDirectory}{Character: directory for output files (default: NULL = same as input)}

\item{output_format}{Character: "AsspDataObj", "dataframe", or "list" (used when toFile=FALSE)}

\item{...}{Additional arguments (reserved for future use)}
}
\value{
If toFile=TRUE, returns output file path. Otherwise returns AsspDataObj with F0 track
(and optionally voicing track), or data.frame/list depending on output_format
}
\description{
Extracts pitch (F0) contour from audio using DisVoice's optimized
Parselmouth implementation. This function uses in-memory processing
and is 1.5-2.5x faster than file-based Praat methods.
}
\details{
This function uses DisVoice's \code{extract_pitch_and_voicing()} which:
\itemize{
\item Loads audio with av (in-memory, no temp files)
\item Processes with Parselmouth Python API
\item Returns F0 values, time stamps, and TextGrid with voicing decisions
\item Converts to equal-interval track format
}

The voicing track is binary: 1 = voiced, 0 = unvoiced

\strong{Performance}: Approximately 2x faster than file-based Praat methods
due to elimination of file I/O overhead.

\strong{Backend}: Python (DisVoice + Parselmouth)
}
\examples{
\dontrun{
# Check if DisVoice is available
if (has_disvoice_support()) {
  # Basic usage
  f0_track <- trk_dv_f0("audio.wav")

  # Custom parameters
  f0_track <- trk_dv_f0(
    "audio.wav",
    frame_shift = 5,  # 5ms = 200 Hz
    min_f0 = 60,
    max_f0 = 500,
    include_voicing = TRUE
  )

  # Get as data.frame
  f0_df <- trk_dv_f0("audio.wav", output_format = "dataframe")
}
}
}
\references{
\insertCite{Jadoul2018}{superassp}

\insertCite{OrozcoArroyave2018}{superassp}
}
\seealso{
\code{\link{has_disvoice_support}} to check availability
\code{\link{install_disvoice_python}} to install dependencies
\code{\link{trk_dv_formants}} for formant tracking
}
