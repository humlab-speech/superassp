% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_pladdrr_dysprosody.R
\name{lst_dysprosody}
\alias{lst_dysprosody}
\title{Extract Dysprosody Prosodic Features}
\usage{
lst_dysprosody(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Vector of file paths to audio files}

\item{beginTime}{Start time in seconds (default: 0.0)}

\item{endTime}{End time in seconds (default: 0.0 = full duration)}

\item{minF}{Minimum F0 in Hz (default: 60)}

\item{maxF}{Maximum F0 in Hz (default: 750)}

\item{windowShift}{Window shift in milliseconds (default: 10)}

\item{toFile}{Write output to .dyp files (default: FALSE)}

\item{explicitExt}{Output file extension (default: "dyp")}

\item{outputDirectory}{Output directory (default: NULL = same as input)}

\item{verbose}{Show progress (default: TRUE)}
}
\value{
If toFile=FALSE: list of data frames (one per file) with 193 features.
If toFile=TRUE: invisibly returns vector of output file paths.
}
\description{
Extracts 193 prosodic features using pladdrr-based dysprosody pipeline including
F0 analysis (MOMEL/INTSINT), intensity, formants, and spectral tilt measures
with Iseli-Alwan harmonic correction.
}
\details{
\strong{Requires}: pladdrr >= 4.8.23

\strong{Features Extracted} (193 total):
\itemize{
\item F0 analysis: MOMEL quadratic spline modeling, INTSINT tonal coding
\item Intensity measures: statistics of intensity contour at INTSINT targets
\item Spectral tilt: 7 measures including Iseli-Alwan harmonic correction (L2L1, L2cL1c, L1cLF3c, L1LF3, SLF, C1, Spectral Balance)
\item Time-series statistics: mean, std, variation, IQR, max, min for all measures and their first differences
\item Global measures: duration, pitch key, pitch range, INTSINT concentration
}

\strong{Performance}: ~10-12 seconds per file (optimized via batch queries)

\strong{Output Format}: When toFile=TRUE, writes JSON Track Format (.dyp) files
with all 193 features organized by time slice.
}
\examples{
\dontrun{
# Extract dysprosody features from audio
result <- lst_dysprosody("speech.wav", toFile = FALSE)

# Access specific features
cat("Duration:", result$Duration, "seconds\n")
cat("Pitch mean:", result$momelpitchtmean, "Hz\n")
cat("Pitch key:", result$PitchKey, "Hz\n")
cat("Pitch range:", result$PitchRange, "octaves\n")

# Batch processing with file output
files <- c("speech1.wav", "speech2.wav")
lst_dysprosody(files, toFile = TRUE, outputDirectory = "output")
}

}
\references{
\insertCite{Villarubia2025}{superassp}
}
