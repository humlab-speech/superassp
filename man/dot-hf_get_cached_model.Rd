% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hf_model_cache.R
\name{.hf_get_cached_model}
\alias{.hf_get_cached_model}
\title{Get a cached path to a Hugging Face Hub model file, downloading if needed}
\usage{
.hf_get_cached_model(repo_id, filename, subdir, revision, repo_type = "model")
}
\arguments{
\item{repo_id}{Hugging Face Hub repo id, e.g. \code{"username/model-name"}.}

\item{filename}{Path to the file inside the repo, e.g. \code{"model.onnx"}.}

\item{subdir}{Cache subdirectory name for this model (keeps different
models from colliding when their filenames share a basename), e.g.
\code{"swift-f0"}.}

\item{revision}{Git revision (tag or commit SHA) to pin. Required, and
deliberately has no default -- pinning to \code{"main"} would let the
model change under the package without a version bump, undermining
DSP output faithfulness.}

\item{repo_type}{One of \code{"model"}, \code{"dataset"}, \code{"space"}.
Default \code{"model"}.}
}
\value{
Character path to the cached local file.
}
\description{
Downloads \code{filename} from the Hugging Face Hub repo \code{repo_id} on
first use and caches it under the R user cache directory
(\code{tools::R_user_dir("superassp", "cache")}); subsequent calls return
the cached path without any network access.
}
\keyword{internal}
