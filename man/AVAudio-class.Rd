% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s7_avaudio.R
\name{AVAudio-class}
\alias{AVAudio-class}
\alias{AVAudio}
\title{AVAudio S7 Class}
\usage{
AVAudio(
  samples = integer(0),
  sample_rate = integer(0),
  channels = integer(0),
  file_path = NA_character_
)
}
\description{
AVAudio is an S7 class that encapsulates audio sample data along with
essential metadata (sample rate, channels). It provides a modern object-oriented
interface for audio processing functions in superassp.
}
\details{
An S7 class for representing audio data with metadata, wrapping the output
of \code{prep_recode()} or \code{av::read_audio_bin()}.

The AVAudio class stores:
\itemize{
\item \code{samples}: Integer vector of audio samples (s32le format - 32-bit signed integers)
\item \code{sample_rate}: Sample rate in Hz (integer)
\item \code{channels}: Number of audio channels (integer)
\item \code{file_path}: Optional source file path (character, NULL if generated)
}

AVAudio objects can be created from:
\enumerate{
\item File paths via \code{prep_recode()} or \code{read_avaudio()}
\item Raw audio data via \code{as_avaudio()}
\item Existing av::read_audio_bin() output via \code{as_avaudio()}
}
}
\examples{
\dontrun{
# Create from file
audio <- read_avaudio("speech.wav")

# Create from prep_recode output
audio_data <- prep_recode("speech.wav", format = "wav")
audio <- as_avaudio(audio_data)

# Access properties
audio$sample_rate
audio$channels
length(audio$samples)

# Use in DSP functions
f0 <- trk_rapt(audio)
features <- lst_voice_sauce(audio)
}

}
