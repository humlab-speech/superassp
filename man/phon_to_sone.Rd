% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iso532_sone.R
\name{phon_to_sone}
\alias{phon_to_sone}
\title{Convert Phon to Sone}
\usage{
phon_to_sone(phon, method = c("zwicker", "moore-glasberg"))
}
\arguments{
\item{phon}{Numeric; loudness level in phons. Must be non-negative.}

\item{method}{Character; conversion method. One of:
\itemize{
\item \code{"zwicker"} (default): ISO 532-1 Zwicker method with piecewise formula
\item \code{"moore-glasberg"}: ISO 532-2 Moore-Glasberg method with lookup table
}}
}
\value{
Numeric vector of loudness values in sones
}
\description{
Convert loudness level (phon) to loudness (sone) using ISO 532 methods.
}
\details{
The Zwicker method (ISO 532-1) uses:
\itemize{
\item For phon < 40: \code{sone = (phon / 40)^(1/0.35)}
\item For phon ≥ 40: \code{sone = 2^((phon - 40) / 10)}
}

The Moore-Glasberg method (ISO 532-2) uses a lookup table with 23 reference
points and log-linear interpolation.

By definition, 1 sone = 40 phons (a 1 kHz tone at 40 dB SPL).
\subsection{Vectorization}{

This function is fully vectorized and can process vectors of phon values
efficiently.
}
}
\examples{
# Reference value
phon_to_sone(40)  # Returns 1.0 (by definition)

# Doubling property: +10 phon ≈ 2× loudness
phon_to_sone(50)  # Returns ~2.0
phon_to_sone(60)  # Returns ~4.0

# Low levels (below 40 phon)
phon_to_sone(20)  # Returns ~0.15

# Vectorized
phon_to_sone(c(20, 40, 60, 80))

# Moore-Glasberg method
phon_to_sone(40, method = "moore-glasberg")

}
\seealso{
\code{\link[=sone_to_phon]{sone_to_phon()}}, \code{\link[=db_and_hz_to_phon]{db_and_hz_to_phon()}}, \code{\link[=phon_and_hz_to_db]{phon_and_hz_to_db()}}
}
