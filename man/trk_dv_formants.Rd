% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_python_dv_formants.R
\name{trk_dv_formants}
\alias{trk_dv_formants}
\title{Track Formant Frequencies using DisVoice}
\usage{
trk_dv_formants(
  audio_path,
  frame_shift = 5,
  window_size = 25,
  max_formants = 5,
  max_formant_freq = 5500,
  toFile = FALSE,
  explicitExt = "dvfm",
  outputDirectory = NULL,
  output_format = c("AsspDataObj", "dataframe", "list"),
  ...
)
}
\arguments{
\item{audio_path}{Character string: path to audio file (any format supported by av)}

\item{frame_shift}{Numeric: frame shift in milliseconds (default: 5ms = 200 Hz)}

\item{window_size}{Numeric: analysis window size in milliseconds (default: 25ms)}

\item{max_formants}{Integer: maximum number of formants to extract (default: 5)}

\item{max_formant_freq}{Numeric: maximum formant frequency in Hz (default: 5500)}

\item{toFile}{Logical: if TRUE, write result to SSFF file (default: FALSE)}

\item{explicitExt}{Character: file extension for output SSFF file (default: "dvfm")}

\item{outputDirectory}{Character: directory for output files (default: NULL = same as input)}

\item{output_format}{Character: "AsspDataObj", "dataframe", or "list" (used when toFile=FALSE)}

\item{...}{Additional arguments (reserved for future use)}
}
\value{
If toFile=TRUE, returns output file path. Otherwise returns AsspDataObj with F1, F2, F3, F4 tracks,
or data.frame/list depending on output_format
}
\description{
Extracts formant frequencies (F1-F4) from audio using DisVoice's optimized
Parselmouth implementation. This function uses in-memory processing
and is approximately 1.5x faster than file-based Praat methods.
}
\details{
This function uses DisVoice's \code{extract_formants()} which:
\itemize{
\item Loads audio with av (in-memory, no temp files)
\item Processes with Parselmouth Python API
\item Returns F1-F4 values and time stamps directly
\item Converts to equal-interval track format
}

\strong{Note}: Unlike the file-based version which returns only F1 and F2,
this function returns F1, F2, F3, and F4.

\strong{Performance}: Approximately 1.5x faster than file-based Praat methods
due to elimination of file I/O overhead.

\strong{Backend}: Python (DisVoice + Parselmouth)
}
\examples{
\dontrun{
# Check if DisVoice is available
if (has_disvoice_support()) {
  # Basic usage
  formant_track <- trk_dv_formants("audio.wav")

  # Custom parameters
  formant_track <- trk_dv_formants(
    "audio.wav",
    frame_shift = 10,      # 10ms = 100 Hz
    window_size = 30,       # 30ms window
    max_formants = 5,
    max_formant_freq = 5000  # For male speakers
  )

  # Access individual formants
  plot(formant_track$tracks[, "F1"], type = "l", col = "blue")
  lines(formant_track$tracks[, "F2"], col = "red")

  # Get as data.frame
  formant_df <- trk_dv_formants("audio.wav", output_format = "dataframe")
}
}
}
\references{
\insertCite{Jadoul2018}{superassp}

\insertCite{OrozcoArroyave2018}{superassp}
}
\seealso{
\code{\link{has_disvoice_support}} to check availability
\code{\link{install_disvoice_python}} to install dependencies
\code{\link{trk_dv_f0}} for F0 tracking
}
