% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assp_dataobj.R
\name{get_track_label}
\alias{get_track_label}
\title{Get track label for plotting}
\usage{
get_track_label(df, col, full = FALSE)
}
\arguments{
\item{df}{data.frame or tibble. Data frame from \code{as.data.frame.AsspDataObj()}.}

\item{col}{Character. Column name.}

\item{full}{Logical. If TRUE, return full descriptive label. If FALSE
(default), return short label suitable for plot axes.}
}
\value{
Character. Display label for the track.
}
\description{
Extracts the display label for a track column, either from stored attributes
or by intelligent inference.
}
\details{
This function first checks the \code{track_labels} or \code{track_descriptions}
attributes stored by \code{as.data.frame.AsspDataObj()}. If not found, it
intelligently infers the label from the column name.

\strong{Short labels} (full = FALSE):
\itemize{
\item "fo \link{Hz}", "F1 \link{Hz}", "H1-H2c \link{dB}"
\item Suitable for plot axes
}

\strong{Full labels} (full = TRUE):
\itemize{
\item "Frequency of oscillation \link{Hz}"
\item "First formant frequency \link{Hz}"
\item "H1-H2 corrected for formants \link{dB}"
\item Suitable for documentation, papers
}
}
\examples{
\dontrun{
fms <- wrassp::forest("audio.wav", toFile = FALSE)
df <- as.data.frame(fms)

# Short label
get_track_label(df, "F1_Hz")
# [1] "F1 [Hz]"

# Full descriptive label
get_track_label(df, "F1_Hz", full = TRUE)
# [1] "First formant frequency [Hz]"

# Works even without attributes (inference)
df2 <- data.frame(frame_time = 1:10, fo_Hz = rnorm(10, 120, 10))
get_track_label(df2, "fo_Hz")
# [1] "fo [Hz]"
}
}
