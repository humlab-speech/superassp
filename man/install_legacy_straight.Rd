% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_legacy_straight.R
\name{install_legacy_straight}
\alias{install_legacy_straight}
\title{Install legacy STRAIGHT Python dependencies}
\usage{
install_legacy_straight(
  method = "auto",
  conda = "auto",
  envname = "r-reticulate",
  install_numba = TRUE,
  ...
)
}
\arguments{
\item{method}{Installation method for Python packages
\itemize{
\item "auto" (default): Automatically selects pip or conda
\item "virtualenv": Uses Python virtualenv
\item "conda": Uses Conda environment
}}

\item{conda}{Name of Conda environment to use (if method = "conda")}

\item{envname}{Name of Python environment (default: "r-reticulate")}

\item{install_numba}{Logical; install Numba for JIT optimization (default: TRUE).
Provides ~20\% speedup for F0 extraction with no code changes.}

\item{...}{Additional arguments passed to \code{reticulate::py_install()}}
}
\value{
Invisible NULL. Called for side effects.
}
\description{
Installs the required Python packages for legacy STRAIGHT vocoder analysis
and synthesis. The STRAIGHT algorithm provides high-quality pitch-adaptive
spectral analysis for speech.
}
\details{
The function installs:
\itemize{
\item numpy (>=1.24.0): Numerical computing
\item scipy (>=1.11.0): Signal processing (FFT, filtering)
\item soundfile (>=0.12.0): Audio I/O
\item matplotlib (>=3.7.0): Visualization (optional)
\item numba (optional): JIT compilation for 20\% speedup
}

The legacy STRAIGHT module is included in the package at
\verb{inst/python/legacy_STRAIGHT/} and does not require separate installation.

\strong{Performance Note}: Installing Numba (\code{install_numba = TRUE}) provides
automatic ~20\% speedup for F0 extraction with zero code changes. The
optimization is transparent and falls back gracefully if Numba is unavailable.
}
\examples{
\dontrun{
# Install with Numba optimization (recommended)
install_legacy_straight(install_numba = TRUE)

# Install minimal dependencies only
install_legacy_straight(install_numba = FALSE)

# Install in specific conda environment
install_legacy_straight(method = "conda", conda = "my-env")
}

}
