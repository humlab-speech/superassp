% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_python_pm_psauce.R
\name{trk_praat_sauce}
\alias{trk_praat_sauce}
\title{Extract voice quality measures using Parselmouth (PraatSauce)}
\usage{
trk_praat_sauce(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Character vector with path(s) to audio file(s)}

\item{beginTime}{Numeric. Start time in seconds (default NULL = 0)}

\item{endTime}{Numeric. End time in seconds (default NULL = end of file)}

\item{windowShift}{Numeric. Time step between measurements in milliseconds (default 5)}

\item{windowSize}{Numeric. Analysis window length in milliseconds (default 25)}

\item{minF}{Numeric. Minimum F0 in Hz (default 50)}

\item{maxF}{Numeric. Maximum F0 in Hz (default 300)}

\item{formantTracking}{Logical. Use formant tracking for cleaner tracks (default TRUE)}

\item{numFormants}{Integer. Number of formants to track (default 5)}

\item{maxFormantHz}{Numeric. Maximum formant frequency in Hz (default 5000)}

\item{nominalF1}{Numeric. Reference F1 frequency for tracking (default 500)}

\item{nominalF2}{Numeric. Reference F2 frequency for tracking (default 1500)}

\item{nominalF3}{Numeric. Reference F3 frequency for tracking (default 2500)}

\item{preEmphFrom}{Numeric. Pre-emphasis frequency in Hz (default 50)}

\item{useBandwidthFormula}{Logical. Use Hawks & Miller bandwidth formula (default FALSE)}

\item{channel}{Integer. Audio channel to extract (default 1)}

\item{resample_to_16k}{Logical. Resample audio to 16kHz (default TRUE)}

\item{toFile}{Logical. Save output to file (default TRUE)}

\item{explicitExt}{Character. File extension for output (default "psa")}

\item{outputDirectory}{Character. Directory for output files (default NULL)}

\item{verbose}{Logical. Print progress messages (default FALSE)}

\item{praat_path}{Character. Path to Praat executable (not used, for compatibility)}
}
\value{
If toFile=TRUE, returns number of files successfully processed.
If toFile=FALSE, returns an AsspDataObj with voice quality measurements at regular intervals:
\describe{
\item{t}{Time in seconds}
\item{f0}{Fundamental frequency in Hz}
\item{F1, F2, F3}{Formant frequencies in Hz}
\item{B1, B2, B3}{Formant bandwidths in Hz}
\item{H1u, H2u, H4u}{Uncorrected harmonic amplitudes (dB)}
\item{H2Ku, H5Ku}{Uncorrected amplitudes at 2kHz and 5kHz (dB)}
\item{A1u, A2u, A3u}{Uncorrected formant amplitudes (dB)}
\item{H1H2u, H2H4u}{Uncorrected harmonic differences (dB)}
\item{H1A1u, H1A2u, H1A3u}{Uncorrected H1-formant differences (dB)}
\item{H2KH5Ku}{Uncorrected spectral tilt measure (dB)}
\item{H1c, H2c, H4c}{Corrected harmonic amplitudes (dB)}
\item{A1c, A2c, A3c}{Corrected formant amplitudes (dB)}
\item{H1H2c, H2H4c}{Corrected harmonic differences (dB)}
\item{H1A1c, H1A2c, H1A3c}{Corrected H1-formant differences (dB)}
\item{CPP}{Cepstral Peak Prominence}
\item{HNR05, HNR15, HNR25, HNR35}{Harmonics-to-Noise Ratio at different frequency bands (dB)}
}
}
\description{
This is a memory-based implementation of \code{\link{praat_sauce}} using
Parselmouth instead of external Praat. It eliminates disk I/O by loading
audio directly into memory using the av package and processing with
Parselmouth.
}
\details{
PraatSauce extracts comprehensive voice quality measures including pitch,
formants, and spectral measures (harmonics, CPP, HNR). This function provides
identical functionality to \code{praat_sauce} but with significantly improved
performance (10-20x faster) by eliminating file I/O operations.
}
\examples{
\dontrun{
# Basic usage
result <- trk_praat_sauce(
  listOfFiles = "audio.wav",
  windowShift = 5,  # 5ms between measurements
  minF = 75,
  maxF = 300
)

# Extract measures from specific portion
result <- trk_praat_sauce(
  listOfFiles = "audio.wav",
  beginTime = 1.0,
  endTime = 3.0,
  windowShift = 10  # 10ms for faster processing
)
}
}
