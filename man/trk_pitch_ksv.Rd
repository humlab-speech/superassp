% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_c_assp_pitch_ksv.R
\name{trk_pitch_ksv}
\alias{trk_pitch_ksv}
\alias{foana}
\alias{fo_ksv}
\alias{trk_ksvfo}
\title{Track fundamental frequency using the KSV periodicity detector}
\usage{
trk_pitch_ksv(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Character vector of audio file paths. Any format supported by
\pkg{av} is accepted; non-native inputs are transcoded automatically.}

\item{beginTime}{Numeric. Start of analysis window in seconds. Default 0 (file start).}

\item{endTime}{Numeric. End of analysis window in seconds. Default 0 (file end).}

\item{windowShift}{Numeric. Frame shift in milliseconds; sets output frame rate
(1000 / windowShift Hz). Default 5 ms.}

\item{gender}{Character. Gender-specific \ifelse{html}{\out{f<sub>o</sub>}}{\eqn{f_o}}
search range: \code{"f"} (female, 80–640 Hz), \code{"m"} (male, 50–400 Hz),
\code{"u"} (unknown, 50–600 Hz, default).}

\item{maxF}{Numeric. Maximum \ifelse{html}{\out{f<sub>o</sub>}}{\eqn{f_o}} in Hz.
Default 600.}

\item{minF}{Numeric. Minimum \ifelse{html}{\out{f<sub>o</sub>}}{\eqn{f_o}} in Hz.
Default 50.}

\item{minAmp}{Numeric. Minimum waveform amplitude threshold for voiced frames.
Default 50.}

\item{maxZCR}{Numeric. Maximum zero-crossing rate in Hz for voicing detection.
Default 3000.0.}

\item{toFile}{Logical. If \code{TRUE}, write SSFF output files and return the
count written (invisibly). If \code{FALSE}, return an \code{AsspDataObj}.
Default \code{FALSE}.}

\item{explicitExt}{Character. Output file extension. Default \code{"fo"}.}

\item{outputDirectory}{Character. Directory for output files. \code{NULL} (default)
writes alongside the input file.}

\item{assertLossless}{Character vector of additional file extensions to treat as
losslessly encoded.}

\item{logToFile}{Logical. Write processing log to a file in \code{outputDirectory}
rather than the console. Default \code{FALSE}.}

\item{convertOverwrites}{Logical. Allow transcoding to overwrite existing files.
Default \code{FALSE}.}

\item{keepConverted}{Logical. Retain intermediate transcoded files. Default \code{FALSE}.}

\item{verbose}{Logical. Print per-file progress. Default \code{FALSE}.}
}
\value{
If \code{toFile = FALSE}: an \code{AsspDataObj} with track:
\describe{
\item{\code{fo[Hz]}}{REAL32, Hz, n_frames x 1 column.
Estimated fundamental frequency; 0 indicates unvoiced frames.}
}
Frame rate: \code{1000 / windowShift} Hz (default 200 Hz).
If \code{toFile = TRUE}: integer count of files written, returned invisibly.
}
\description{
Estimates the fundamental frequency \ifelse{html}{\out{f<sub>o</sub>}}{\eqn{f_o}}
using the Schäfer-Vincent periodicity detection algorithm
\insertCite{Schäfer-Vincent.1983.10.1159/000261691}{superassp} implemented in
the \emph{libassp} C library \insertCite{s5h}{superassp}. This extremum-based
method is fast and works directly on the waveform without spectral analysis.
}
\details{
\code{gender} sets the default \code{minF}/\code{maxF} search range but is
overridden by explicit \code{minF}/\code{maxF} values. \code{minAmp} and
\code{maxZCR} control voicing detection independently of the pitch range.
}
\examples{
# get path to audio file
path2wav <- list.files(system.file("samples","sustained", package = "superassp"), pattern = glob2rx("a1.wav"), full.names = TRUE)

# calculate fundamental frequency contour
res <- trk_pitch_ksv(path2wav, toFile=FALSE)

# plot the fundamental frequency contour
plot(seq(0,numRecs.AsspDataObj(res) - 1) / rate.AsspDataObj(res) +
      attr(res, 'startTime'),
    res[["fo[Hz]"]],
    type='l',
    xlab='time (s)',
    ylab=expression(paste(f[o]," frequency (Hz)")))

}
\references{
\insertAllCited{}
}
\seealso{
\link[wrassp:ksvF0]{wrassp::ksvF0}
}
\author{
Raphael Winkelmann

Lasse Bombien

Fredrik Nylén
}
