% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggtrack_geoms.R
\name{geom_spectrogram}
\alias{geom_spectrogram}
\title{Plot a spectral track as a spectrogram}
\usage{
geom_spectrogram(
  mapping = NULL,
  data = NULL,
  ...,
  tracks = NULL,
  time = "frame_time",
  freq_hz_per_bin = NULL,
  na.zeros = FALSE,
  na.rm = FALSE,
  interpolate = FALSE,
  show.legend = NA,
  inherit.aes = FALSE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}},
applied to the time x frequency table the layer prepares (\code{frame_time},
\code{freq}, \code{value}). When \code{NULL} (default) the spectrum is drawn with
\code{aes(x = frame_time, y = freq, fill = value)}.}

\item{data}{The data to display: an \code{AsspDataObj} holding a spectrum (from
\code{\link[=trk_dft_spectrum]{trk_dft_spectrum()}}, \code{\link[=trk_lps_spectrum]{trk_lps_spectrum()}}, \code{\link[=trk_css_spectrum]{trk_css_spectrum()}} or any
other multi-column track), the wide table from
\code{\link[=as.data.frame.AsspDataObj]{as.data.frame.AsspDataObj()}}, or an already long table. Inherited from the
plot when \code{NULL}.}

\item{...}{Other arguments passed on to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{tracks}{Character. The spectral track to draw when \code{data} holds more
than one multi-column track. \code{NULL} (default) uses the only one.}

\item{time}{Character. Preferred name of the time column of \code{data}. The
plotted table always names it \code{frame_time}.}

\item{freq_hz_per_bin}{Numeric. Spacing between the coefficients in Hz,
overriding the spacing derived from the \code{origFreq} of the object. Needed
when \code{data} carries no sample rate.}

\item{na.zeros}{Logical. Convert stored zeros to \code{NA}, leaving those raster
cells unfilled. Default: \code{FALSE}.}

\item{na.rm}{Logical. Remove \code{NA} values before drawing. Default: \code{FALSE}.}

\item{interpolate}{Logical. Interpolate the raster for a smoother image.
Default: \code{FALSE}.}

\item{show.legend, inherit.aes}{Passed on to \code{\link[ggplot2:layer]{ggplot2::layer()}}, with
ggplot2's usual defaults except \code{inherit.aes = FALSE} (see Details).}
}
\value{
A ggplot2 layer.
}
\description{
A ggplot2 layer that draws a multi-column spectral track of an \code{AsspDataObj}
as a time x frequency raster, with the track values (dB for the package's
spectra) on the fill scale.
}
\details{
SSFF stores a spectrum from 0 Hz up to the Nyquist rate, so the coefficients
of a track with \code{n} columns sit at \verb{0, bin_hz, ..., (n - 1) * bin_hz} with
\code{bin_hz = origFreq / (2 * (n - 1))}: a 2048-point spectrum of 44.1 kHz audio
has 1025 bins of ~21.5 Hz. The track must be a frequency-domain track and
\code{data} must hold exactly one multi-column track, or \code{tracks} must pick it;
multi-column tracks of anything else (formants, LPC coefficients) belong to
\code{\link[=geom_track]{geom_track()}}.
The raster is drawn by \code{\link[ggplot2:geom_raster]{ggplot2::geom_raster()}}'s engine, which needs evenly
spaced times and frequencies (all spectra in this package are evenly spaced).
Zoom with \code{\link[ggplot2:coord_cartesian]{ggplot2::coord_cartesian()}} rather than scale limits to keep the
raster intact.
}
\examples{
wav <- system.file("samples", "sustained", "a1.wav", package = "superassp")
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  library(ggplot2)

  dft <- trk_dft_spectrum(wav, toFile = FALSE, verbose = FALSE)
  ggplot() +
    geom_spectrogram(data = dft) +
    coord_cartesian(ylim = c(0, 5000))

  # an object inherited from the plot works the same way
  lps <- trk_lps_spectrum(wav, toFile = FALSE, verbose = FALSE)
  ggtrack(lps) +
    geom_spectrogram() +
    labs(y = "Frequency [Hz]") +
    scale_fill_viridis_c()
}
}
}
\seealso{
\code{\link[=geom_track]{geom_track()}} for single-value tracks and waveforms, \code{\link[=ggtrack]{ggtrack()}}
for automatic track labels.
}
