% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_covarep_hmpd.R
\name{trk_hmpd}
\alias{trk_hmpd}
\title{HMPD Phase Distortion Features (COVAREP)}
\usage{
trk_hmpd(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Vector of file paths (WAV, MP3, MP4, etc.) to analyze}

\item{f0s}{Optional matrix with 2 columns (time, f0_hz). If NULL, computed internally via trk_srh_variant}

\item{beginTime}{Start time in seconds (0 for beginning of file)}

\item{endTime}{End time in seconds (0 for end of file)}

\item{f0min}{Minimum F0 for search (Hz, default: 60)}

\item{f0max}{Maximum F0 for search (Hz, default: 440)}

\item{toFile}{Write output to file (TRUE) or return object (FALSE). Default: FALSE}

\item{explicitExt}{Output file extension (default: "hpd")}

\item{outputDirectory}{Output directory (NULL for same as input file)}

\item{verbose}{Show progress messages (default: TRUE)}
}
\value{
If \code{toFile=FALSE} (default): AsspDataObj with 3 matrix tracks:
\itemize{
\item \code{ae}: 24-dim amplitude envelope (Mel cepstral, method=2, log-lin)
\item \code{pdm}: 24-dim phase deviation mean (log-lin, log-harmonics)
\item \code{pdd}: 12-dim phase deviation deviation (log magnitude)
}

If \code{toFile=TRUE}: invisibly returns vector of output file paths
}
\description{
Computes HMPD Phase Distortion vocoder features: amplitude envelope (AE),
phase deviation mean (PDM), phase deviation deviation (PDD) at frame level.
Performs sinusoidal analysis + harmonic phase distortion quantification.
}
\details{
\strong{Algorithm} \insertCite{Degottex2014COVAREP}{superassp}:
\enumerate{
\item Compute F0 track (internally via SRH if not provided externally)
\item Sinusoidal analysis: extract harmonics from each frame's complex spectrum
\item Amplitude envelope: log-Mel-cepstrum representation (24 coeffs)
\item Phase tracking: harmonic phase per frame
\item Phase deviation mean (PDM): within-frame phase dynamics (24 coeffs, log-linear)
\item Phase deviation deviation (PDD): phase variability across harmonics (12 coeffs)
}

\strong{Output format}: Compressed (60 features/frame total: 24+24+12)
\itemize{
\item ae: non-log amplitude envelope
\item pdm: log-harmonic scale, mean phase
\item pdd: log-harmonic scale, phase deviation magnitude
}

\strong{Interpretation}:
\itemize{
\item Large PDD = aperiodicity, noise-like phase scatter per harmonic
\item Structured PDM = deterministic phase dynamics (e.g., vibrato)
\item AE = spectral envelope shape (low frequencies dominated in voice)
}
}
\examples{
\dontrun{
# Single file, return object
hmpd <- trk_hmpd("speech.wav", toFile = FALSE)

# Batch process multiple files
files <- c("file1.wav", "file2.wav")
hmpd_results <- trk_hmpd(files, toFile = TRUE, outputDirectory = "output/")
}

}
\references{
\insertAllCited{}
}
