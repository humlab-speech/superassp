% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_covarep_vad_drugman.R
\name{trk_covarep_vad_drugman}
\alias{trk_covarep_vad_drugman}
\title{Multi-Branch Voice Activity Detection (Drugman)}
\usage{
trk_covarep_vad_drugman(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Vector of file paths (WAV, MP3, MP4, etc.) to analyze}

\item{beginTime}{Start time in seconds (0 for beginning of file)}

\item{endTime}{End time in seconds (0 for end of file)}

\item{toFile}{Write output to file (TRUE) or return object (FALSE). Default: FALSE}

\item{explicitExt}{Output file extension (default: "cvd")}

\item{outputDirectory}{Output directory (NULL for same as input file)}

\item{verbose}{Show progress messages (default: TRUE)}
}
\value{
If \code{toFile=FALSE} (default): AsspDataObj with 4 tracks: vad_final, vad_mfcc,
vad_sadjadi, vad_new (all 0-1 voicing probability at 5ms frame shift).

If \code{toFile=TRUE}: invisibly returns vector of output file paths
}
\description{
Computes voice activity detection posteriors using Drugman's multi-branch approach.
Combines MFCC, Sadjadi, and new feature ANN branches for robust VAD.
}
\details{
\strong{Multi-Branch VAD} \insertCite{Drugman2012VAD}{superassp}:
\itemize{
\item \strong{MFCC branch}: 13 MFCCs + harmonic/clarity/LPC features
\item \strong{Sadjadi branch}: 4 Sadjadi features (pitch-related)
\item \strong{New branch}: 3 new features (CPP + SRH variants)
\item All features processed through separate ANNs
\item Final output: geometric mean of 3 branch posteriors
}

\strong{Frame parameters}:
\itemize{
\item Window: 30ms (Hann)
\item Hop size: 10ms (interpolated to 5ms output)
\item Target sample rate: 16kHz (auto-resampled)
}

\strong{Interpretation}:
\itemize{
\item \strong{vad_final}: Ensemble posterior (0-1, recommended for use)
\item \strong{vad_mfcc}: MFCC-based posterior (spectral features)
\item \strong{vad_sadjadi}: Sadjadi posterior (pitch-based features)
\item \strong{vad_new}: Novel feature posterior (alternative features)
}

Typical thresholds: >0.7 (high confidence voiced), 0.3-0.7 (ambiguous), <0.3 (unvoiced)

\strong{Use cases}:
\itemize{
\item Speech/music separation (classify music frames as non-speech)
\item Robust F0 tracking initialization (skip non-voice frames)
\item Audio preprocessing (noise gate, speech detection)
}
}
\examples{
\dontrun{
# Single file
vad <- trk_covarep_vad_drugman("speech.wav", toFile = FALSE)

# Batch processing
files <- c("file1.wav", "file2.wav")
trk_covarep_vad_drugman(files, toFile = TRUE, outputDirectory = "vad/")
}

}
\references{
\insertAllCited{}
}
