% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_polarity.R
\name{lst_polarity}
\alias{lst_polarity}
\title{Signal polarity detection (RESKEW algorithm)}
\usage{
lst_polarity(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Vector of file paths (WAV, MP3, MP4, etc.) to analyze}

\item{beginTime}{Start time in seconds (0 for beginning of file)}

\item{endTime}{End time in seconds (0 for end of file)}

\item{toFile}{Write output to file (default: FALSE, not supported for scalar output)}

\item{verbose}{Show progress messages (default: TRUE)}
}
\value{
Data frame with columns:
\itemize{
\item \code{file}: Input file basename
\item \code{polarity}: Polarity sign (+1 or -1)
}
}
\description{
Detects microphone/recording polarity from LP residual skewness.
The RESKEW algorithm compares residual skewness characteristics with
and without high-pass filtering to determine signal polarity.
}
\details{
\strong{Algorithm} (RESKEW, Drugman et al.):
\enumerate{
\item High-pass filter signal at 490 Hz cutoff (removes low-frequency noise/drift)
\item Compute LP residual with order = fs/1000 + 2 samples, 25ms frames, 5ms shift
\item Compare residual skewness: unfiltered vs. high-pass filtered
\item Polarity = sign(skew_filtered - skew_unfiltered)
}

\strong{Interpretation}:
\itemize{
\item \code{+1}: Normal polarity (positive peaks are vocal pulses)
\item \code{-1}: Inverted polarity (flip signal before further processing)
}

\strong{Note}: If skewness values are very close, result may be unstable.
Recommend confidence threshold: |result| > 0.1 for reliable polarity detection.
}
\examples{
\dontrun{
# Single file
pol <- lst_polarity("speech.wav")
cat("Polarity:", pol$polarity, "\n")

# Batch process
files <- c("file1.wav", "file2.wav")
polarities <- lst_polarity(files)
}

}
