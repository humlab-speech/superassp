% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_cpp_pyin.R
\name{trk_pyin}
\alias{trk_pyin}
\title{Probabilistic YIN Pitch Tracking (Simplified C++ implementation)}
\usage{
trk_pyin(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Vector of file paths to process}

\item{beginTime}{Start time in seconds (default: 0.0)}

\item{endTime}{End time in seconds (default: 0.0 = end of file)}

\item{windowShift}{Frame shift in milliseconds (default: 5.0)}

\item{windowSize}{Window size in milliseconds (default: 30.0)}

\item{minF}{Minimum F0 in Hz (default: 70.0)}

\item{maxF}{Maximum F0 in Hz (default: 200.0)}

\item{threshold}{Voicing threshold (default: 0.1, lower = more permissive)}

\item{toFile}{Write results to file (default: FALSE)}

\item{explicitExt}{Output file extension (default: "pyp")}

\item{outputDirectory}{Output directory (default: NULL = same as input)}

\item{verbose}{Show progress messages (default: TRUE)}
}
\value{
If toFile=TRUE, returns output file path(s) invisibly.
If toFile=FALSE, returns AsspDataObj or list of AsspDataObj objects.
Each object contains two tracks: "F0" (Hz) and "prob" (voicing probability).
}
\description{
Extract F0 (fundamental frequency) using a simplified probabilistic YIN.
This is a C++ implementation that is significantly faster than the Python/librosa
version and requires no Python dependencies.

The probabilistic YIN algorithm \insertCite{Mauch.2014.10.1109/icassp.2014.6853678}{superassp}
extends YIN \insertCite{Cheveigné.2002.10.1121/1.1458024}{superassp} by considering
multiple pitch candidates. This simplified version provides the core YIN functionality
without the full HMM inference.

All input media formats are supported via the av package, including video
files from which audio will be automatically extracted.
}
\examples{
\dontrun{
# Extract F0 from audio file
f0_data <- trk_pyin("recording.wav", toFile = FALSE)

# Process with custom parameters
trk_pyin("speech.mp3", minF = 75, maxF = 300, windowShift = 10)

# Process video file (extracts audio)
trk_pyin("interview.mp4", toFile = FALSE)
}
}
\references{
\insertAllCited{}
}
