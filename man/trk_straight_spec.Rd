% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_python_straight_spec.R
\name{trk_straight_spec}
\alias{trk_straight_spec}
\title{STRAIGHT Spectral Analysis (Legacy Vocoder)}
\usage{
trk_straight_spec(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Character vector of audio file paths, or AVAudio object}

\item{...}{Additional arguments (for S7 dispatch compatibility)}

\item{beginTime}{Numeric; start time in seconds (default: 0, start of file)}

\item{endTime}{Numeric; end time in seconds (default: 0, end of file)}

\item{f0_floor}{Numeric; minimum F0 in Hz (default: 40)}

\item{f0_ceil}{Numeric; maximum F0 in Hz (default: 800)}

\item{fft_size}{Numeric; FFT size for spectral analysis (default: 2048)}

\item{frame_shift}{Numeric; frame shift in milliseconds (default: 1.0)}

\item{toFile}{Logical; write output to SSFF file (default: FALSE for in-memory)}

\item{explicitExt}{Character; file extension for output (default: "strspec")}

\item{outputDirectory}{Character; output directory (default: NULL, same as input)}

\item{verbose}{Logical; print progress messages (default: TRUE)}
}
\value{
If \code{toFile = FALSE}: AsspDataObj (single file) or list of AsspDataObj
(multiple files) with spectral tracks.
If \code{toFile = TRUE}: Character vector of output file paths (returned invisibly).
}
\description{
Extracts pitch-adaptive spectral envelope using the legacy STRAIGHT algorithm.
Provides 99\% correlation with the original MATLAB implementation.
}
\details{
The STRAIGHT spectral analysis algorithm performs pitch-adaptive smoothing
of the time-frequency representation, providing a high-quality spectral
envelope suitable for vocoding and voice conversion.

\strong{Features}:
\itemize{
\item Pitch-adaptive smoothing (no voicing artifacts)
\item High-resolution spectral representation
\item Compatible with STRAIGHT synthesis
}

\strong{Accuracy}: 99\% correlation with MATLAB STRAIGHT spectral output

\strong{Audio Loading}: Uses \code{av} package for universal media support.
}
\examples{
\dontrun{
# Check availability
if (!straight_available()) {
  install_legacy_straight()
}

# Single file spectral analysis
wav_file <- system.file("samples", "sustained", "a1.wav", package = "superassp")
spec_data <- trk_straight_spec(wav_file, toFile = FALSE)

# Plot spectrogram
image(t(spec_data$spec), col = heat.colors(256))

# Batch processing
files <- list.files("audio/", pattern = "\\\\.wav$", full.names = TRUE)
spec_results <- trk_straight_spec(files, toFile = FALSE)
}

}
\references{
\insertRef{Kawahara.1999.SpeechCommunication}{superassp}
}
\seealso{
\code{\link{trk_straight_f0}}, \code{\link{straight_synth}},
\code{\link{install_legacy_straight}}
}
