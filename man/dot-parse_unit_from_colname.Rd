% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_helpers.R
\name{.parse_unit_from_colname}
\alias{.parse_unit_from_colname}
\title{Parse unit from column name}
\usage{
.parse_unit_from_colname(col_name)
}
\arguments{
\item{col_name}{Character. Column name to parse.}
}
\value{
Character. Unit name (e.g., "Hz", "dB") or NA if no unit found.
}
\description{
Extracts the unit suffix from a cleaned column name.
}
\details{
Handles both track-name conventions used across the package:
\itemize{
\item Bracket form (raw track names): \code{fo[Hz]} → "Hz", \code{F1[Hz]} → "Hz"
\item Cleaned underscore form: \code{fo_Hz} → "Hz", \code{H1_H2c_dB} → "dB"
}

For the underscore form only \emph{known} units are recognised, so ordinary
underscored names are not mistaken for units:
\itemize{
\item \code{frame_time} → NA (\code{time} is not a unit)
\item \code{fm_1} → NA
\item \code{intensity} → NA (no unit)
}
}
\examples{
\dontrun{
.parse_unit_from_colname("fo[Hz]")       # "Hz"
.parse_unit_from_colname("fo_Hz")        # "Hz"
.parse_unit_from_colname("CPP_dB")       # "dB"
.parse_unit_from_colname("frame_time")   # NA
}
}
\keyword{internal}
