% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_cpp_sptk_reaper.R
\name{reaper}
\alias{reaper}
\title{REAPER Pitch and Epoch Tracking (C++ implementation)}
\usage{
reaper(
  listOfFiles = NULL,
  beginTime = 0,
  endTime = 0,
  windowShift = 10,
  minF = 60,
  maxF = 400,
  voicing_threshold = 0.9,
  toFile = TRUE,
  explicitExt = "f0",
  outputDirectory = NULL,
  verbose = TRUE
)
}
\arguments{
\item{listOfFiles}{Vector of file paths to process}

\item{beginTime}{Start time in seconds (default: 0.0)}

\item{endTime}{End time in seconds (default: 0.0 = end of file)}

\item{windowShift}{Frame shift in milliseconds (default: 10.0)}

\item{minF}{Minimum F0 in Hz (default: 60.0)}

\item{maxF}{Maximum F0 in Hz (default: 400.0)}

\item{voicing_threshold}{Voicing threshold (default: 0.9)}

\item{toFile}{Write results to file (default: TRUE)}

\item{explicitExt}{Output file extension (default: "f0")}

\item{outputDirectory}{Output directory (default: NULL = same as input)}

\item{verbose}{Show progress messages (default: TRUE)}
}
\value{
If toFile=TRUE, returns the number of successfully processed files.
If toFile=FALSE, returns AsspDataObj or list of AsspDataObj objects with
f0 track and optionally epochs.
}
\description{
Extract F0 and glottal closure instants using the Robust Epoch
And Pitch EstimatoR (REAPER) from SPTK. This C++ implementation is 2-3x
faster than the Python version.

REAPER provides both F0 estimates and epoch marks (glottal closure instants),
making it useful for voice source analysis.
}
\examples{
\dontrun{
# Extract F0 and epochs
reaper("recording.wav")

# Get epochs for voice source analysis
result <- reaper("speech.wav", toFile = FALSE)
epochs <- attr(result, "epochs")
}
}
