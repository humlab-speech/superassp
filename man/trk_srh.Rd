% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vat_srh.R
\name{trk_srh}
\alias{trk_srh}
\title{SRH F0 Tracking via Voice Analysis Toolkit}
\usage{
trk_srh(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Character vector of audio file paths}

\item{...}{Additional arguments (reserved for future use)}

\item{beginTime}{Numeric vector of start times in seconds (default: 0.0)}

\item{endTime}{Numeric vector of end times in seconds (default: 0.0, full file)}

\item{windowShift}{Frame shift in milliseconds (default: 10.0)}

\item{minF}{Minimum F0 in Hz (default: 20)}

\item{maxF}{Maximum F0 in Hz (default: 500)}

\item{toFile}{Logical; write SSFF files (default: TRUE)}

\item{explicitExt}{File extension for output (default: "f0")}

\item{outputDirectory}{Output directory (default: NULL, same as input)}

\item{verbose}{Show progress messages (default: TRUE)}
}
\value{
If toFile=FALSE: AsspDataObj or list of AsspDataObj with tracks:
\itemize{
\item \code{F0[Hz]}: Estimated fundamental frequency
\item \code{VUV}: Voice/unvoiced decision (0=unvoiced, 1=voiced)
\item \code{SRH}: SRH confidence values
}
If toFile=TRUE: Character vector of output file paths
}
\description{
Estimate fundamental frequency using the Summation of Residual Harmonics (SRH)
algorithm with improved MATLAB faithfulness (>95\% correlation).
}
\details{
This implementation uses the enhanced Python Voice Analysis Toolkit with:
\itemize{
\item Custom LPC analysis (lpcauto) matching MATLAB
\item Symmetric window functions
\item Improved filter initial conditions
}
\subsection{Algorithm}{

The SRH algorithm \insertCite{Drugman2011SRH}{superassp}:
\enumerate{
\item Computes LP residual of speech signal
\item Analyzes harmonic structure in frequency domain
\item Sums energies at harmonic frequencies
\item Subtracts energies at subharmonic frequencies
\item Selects F0 with maximum SRH value
}
}

\subsection{Improvements Over scipy.signal.lpc}{

This implementation achieves >95\% correlation with MATLAB by:
\itemize{
\item Using \code{lpcauto()} instead of \code{scipy.signal.lpc()}
\item Symmetric windows (\code{sym=True})
\item Better filter initial conditions (\code{lfiltic})
}
}

\subsection{Performance}{

Processes 10s audio in ~100ms (100x real-time)
}

\subsection{Requirements}{
\itemize{
\item Python with numpy, scipy, soundfile
\item Install with: \code{install_vat()}
}
}
}
\examples{
\dontrun{
# Check availability
if (!vat_available()) install_vat()

# Single file
f0 <- trk_srh("audio.wav", toFile = FALSE)
plot(f0$`F0[Hz]`, type = "l", ylab = "F0 (Hz)")

# Batch processing
files <- c("speaker1.wav", "speaker2.wav")
trk_srh(files, minF = 75, maxF = 300)

# Female speaker with custom range
f0_female <- trk_srh("female.wav",
                         minF = 150, maxF = 400,
                         toFile = FALSE)
}

}
\references{
\insertCite{Drugman2011SRH}{superassp}

\insertCite{Kane2013VAT}{superassp}
}
\seealso{
\code{\link{install_vat}} for installation,
\code{\link{vat_info}} for availability check,
\code{\link{trk_vat_iaif}} for glottal inverse filtering
}
