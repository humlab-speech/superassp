% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_cpp_crepe.R
\name{trk_crepe}
\alias{trk_crepe}
\title{Compute pitch and periodicity using the CREPE pitch tracker (C++ ONNX)}
\usage{
trk_crepe(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{A vector of file paths to audio files (any format
supported by av).}

\item{beginTime}{Start time (seconds) of the section to process. Default 0.}

\item{endTime}{End time (seconds) of the section to process. Default 0
(= entire file).}

\item{windowShift}{Measurement interval (frame duration) in milliseconds.
Default 10.}

\item{windowSize}{Filter window size in milliseconds for median/mean
smoothing. Default 15.}

\item{minF}{Minimum F0 frequency (Hz). Frames below this are zeroed.
Default 50.}

\item{maxF}{Maximum F0 frequency (Hz). Frames above this are zeroed.
Default 550.}

\item{voicing.threshold}{Periodicity threshold for voicing decisions.
Frames with periodicity below this value are set to unvoiced (F0 = 0).
Default 0.21.}

\item{silence.threshold}{A-weighted dB threshold relative to global maximum.
Frames below this are treated as silent (periodicity = 0). Default -60.}

\item{model}{Model variant: \code{"tiny"} (fast) or \code{"full"} (accurate).
Default \code{"tiny"}.}

\item{decoder}{Decoding method: \code{"viterbi"} (default, recommended) or
\code{"argmax"} (faster but noisier).}

\item{batch_size}{Number of frames per ONNX inference batch. Default 512.}

\item{explicitExt}{Output file extension. Default \code{"crp"}.}

\item{outputDirectory}{Directory for output files. Default NULL (same as
input).}

\item{toFile}{If TRUE, write SSFF files to disk. If FALSE, return
AsspDataObj. Default TRUE.}

\item{verbose}{Print progress messages. Default TRUE.}
}
\value{
If \code{toFile = FALSE}: an AsspDataObj with tracks \code{f0}
(REAL32, Hz) and \code{periodicity} (REAL32, 0-1). If
\code{toFile = TRUE}: the number of files written (invisibly).
}
\description{
The CREPE \insertCite{Kim.2018.10.1109/icassp.2018.8461329}{superassp}
applies a deep convolutional neural network directly on the time-domain
waveform to estimate the fundamental frequency. Two model variants are
available: "tiny" (fast, ~1.9 MB) and "full" (more accurate, ~85 MB).
}
\details{
This implementation uses C++ ONNX Runtime inference — no Python or PyTorch
required. Audio is loaded via the \code{av} package, supporting all media
formats (WAV, MP3, MP4, video files, etc.).

Post-processing chain (matching torchcrepe):
\enumerate{
\item Median filter on periodicity
\item A-weighted silence detection via pladdrr spectrogram
\item Voicing threshold (set F0 to 0 where periodicity < threshold)
\item NaN-aware mean filter on F0
\item F0 range clamping (minF/maxF)
}

ONNX Runtime is automatically installed on first use if not already present.
The runtime library (~30 MB) is cached in your R user directory and persists
across R sessions and package reinstalls.
}
\references{
\insertAllCited{}
}
