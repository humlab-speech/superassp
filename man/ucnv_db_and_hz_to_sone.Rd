% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iso532_sone.R
\name{ucnv_db_and_hz_to_sone}
\alias{ucnv_db_and_hz_to_sone}
\title{Convert dB and Hz Directly to Sone}
\usage{
ucnv_db_and_hz_to_sone(
  spl_db,
  freq_hz,
  method = c("zwicker", "moore-glasberg")
)
}
\arguments{
\item{spl_db}{Numeric; sound pressure level in dB SPL (re 20 μPa)}

\item{freq_hz}{Numeric; frequency in Hz (20-12500 Hz)}

\item{method}{Character; ISO 532 method for phon-to-sone conversion.
One of \code{"zwicker"} (default) or \code{"moore-glasberg"}.}
}
\value{
Numeric vector of loudness values in sones
}
\description{
Convenience function to convert sound pressure level (dB) and frequency (Hz)
directly to loudness (sone), combining ISO 226:2023 and ISO 532 conversions.
}
\details{
This function combines two conversions:
\enumerate{
\item (SPL, frequency) → phon using ISO 226:2023 equal-loudness contours
\item phon → sone using ISO 532 Zwicker or Moore-Glasberg method
}

Equivalent to: \code{ucnv_phon_to_sone(ucnv_db_and_hz_to_phon(spl_db, freq_hz), method)}
}
\examples{
# At 1 kHz reference
ucnv_db_and_hz_to_sone(40, 1000)  # Returns 1.0 (by definition)
ucnv_db_and_hz_to_sone(50, 1000)  # Returns ~2.0 (+10 dB ≈ 2× loudness)

# Low frequency requires more SPL for same loudness
ucnv_db_and_hz_to_sone(60, 100)   # Lower sone value
ucnv_db_and_hz_to_sone(60, 1000)  # Higher sone value

}
\seealso{
\code{\link[=ucnv_db_and_hz_to_phon]{ucnv_db_and_hz_to_phon()}}, \code{\link[=ucnv_phon_to_sone]{ucnv_phon_to_sone()}}
}
