% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track_labels_plotmath.R
\name{get_track_label_expr}
\alias{get_track_label_expr}
\title{Get track label as expression for plotting}
\usage{
get_track_label_expr(df, col, full = FALSE, use_subscripts = TRUE)
}
\arguments{
\item{df}{data.frame or tibble. Data frame from \code{as.data.frame.AsspDataObj()}.}

\item{col}{Character. Column name.}

\item{full}{Logical. If TRUE, return full descriptive label. Default: FALSE.}

\item{use_subscripts}{Logical. If TRUE, return plotmath expression with
subscripts. If FALSE, return plain text. Default: TRUE.}
}
\value{
Expression object (if use_subscripts = TRUE) or character string.
}
\description{
Extended version of \code{get_track_label()} that returns plotmath expressions
with subscripts when \code{use_subscripts = TRUE}.
}
\details{
This function extends \code{get_track_label()} with the option to generate
plotmath expressions for scientific subscripts in ggplot2.

\strong{With subscripts} (use_subscripts = TRUE):
\itemize{
\item fo → f\link{o} (f subscript o)
\item F1 → F\link{1} (F subscript 1)
\item H1-H2c → H\link{1}-H\link{2c} (proper subscripts)
}

\strong{Without subscripts} (use_subscripts = FALSE):
\itemize{
\item Same as \code{get_track_label()}
\item Returns: "fo \link{Hz}", "F1 \link{Hz}", "H1-H2c \link{dB}"
}

For full descriptive labels (full = TRUE), subscripts are not used as the
full text doesn't need them (e.g., "Frequency of oscillation \link{Hz}").
}
\examples{
\dontrun{
fms <- wrassp::forest("audio.wav", toFile = FALSE)
df <- as.data.frame(fms)

# Get expression with subscript
expr <- get_track_label_expr(df, "F1_Hz")
# Returns: expression(F[1]~"[Hz]")

# Use in ggplot
library(ggplot2)
ggplot(df, aes(x = frame_time, y = F1_Hz)) +
  geom_line() +
  labs(y = get_track_label_expr(df, "F1_Hz"))
# Y-axis shows: F₁ [Hz] (with subscript)

# Without subscripts
label <- get_track_label_expr(df, "F1_Hz", use_subscripts = FALSE)
# Returns: "F1 [Hz]"
}
}
