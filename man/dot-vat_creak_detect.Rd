% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vat_internal_creak.R
\name{.vat_creak_detect}
\alias{.vat_creak_detect}
\title{Creaky voice detection
Runs feature extraction + the Kane-Drugman ANN classifier (if weights are
bundled in \code{inst/extdata/creak_ann.rds}) to produce posterior
probabilities and binary decisions per 10 ms frame.}
\usage{
.vat_creak_detect(x, fs, threshold = 0.3, backend = c("cpp", "r"))
}
\arguments{
\item{x}{Speech signal (numeric vector)}

\item{fs}{Sampling frequency (Hz)}

\item{threshold}{ANN decision threshold (default 0.3, matches MATLAB)}

\item{backend}{"cpp" (default, bit-faithful Kane-Drugman + Ishi port)
or "r" (legacy lighter R-side approximation).}
}
\value{
List with: \code{posterior} (probabilities per frame),
\code{decision} (binary), \code{time} (frame center times in s).
}
\description{
Creaky voice detection
Runs feature extraction + the Kane-Drugman ANN classifier (if weights are
bundled in \code{inst/extdata/creak_ann.rds}) to produce posterior
probabilities and binary decisions per 10 ms frame.
}
