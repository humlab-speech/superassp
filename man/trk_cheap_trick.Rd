% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_cpp_world_cheap_trick.R
\name{trk_cheap_trick}
\alias{trk_cheap_trick}
\title{CheapTrick Spectral Envelope Estimation (WORLD vocoder, C++ implementation)}
\usage{
trk_cheap_trick(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Character vector of audio file paths.}

\item{beginTime}{Numeric. Start of analysis window in seconds. Default 0.}

\item{endTime}{Numeric. End of analysis window in seconds. Default 0 (file end).}

\item{windowShift}{Numeric. Frame shift in milliseconds. Default 5.0 ms.}

\item{minF}{Numeric. Minimum F0 in Hz for the Harvest pitch extractor.
Default 60.0 Hz.}

\item{maxF}{Numeric. Maximum F0 in Hz for the Harvest pitch extractor.
Default 400.0 Hz.}

\item{voicing_threshold}{Numeric. Voicing threshold for Harvest (default 0.1).}

\item{q1}{Numeric. CheapTrick spectral regularization parameter (default -0.15).}

\item{f0_floor}{Numeric. Lower F0 bound used to determine FFT size (default 71.0).
For 16 kHz audio, 71 Hz gives fft_size = 2048 (sp_length = 1025).}

\item{toFile}{Logical. If \code{TRUE}, write SSFF output and return count
invisibly. If \code{FALSE}, return \code{AsspDataObj}. Default \code{TRUE}.}

\item{explicitExt}{Character. Output file extension. Default \code{"sp"}.}

\item{outputDirectory}{Character. Output directory. \code{NULL} (default)
writes alongside the input file.}

\item{verbose}{Logical. Print per-file progress. Default \code{TRUE}.}
}
\value{
If \code{toFile = FALSE}: an \code{AsspDataObj} with track:
\describe{
\item{\code{sp}}{REAL32, power spectral envelope, n_frames x (fft_size/2+1).}
}
Frame rate: \code{1000 / windowShift} Hz.
If \code{toFile = TRUE}: integer count of files written, returned invisibly.
}
\description{
Estimate the spectral envelope using the CheapTrick algorithm
from the WORLD vocoder (Morise 2015). CheapTrick uses a Hanning-windowed,
F0-adaptive spectral analysis to produce a smooth, high-quality power
spectral envelope per frame.

Internally: F0 is extracted via Harvest (WORLD), then fed to CheapTrick.
The output is a multi-column SSFF track (\code{sp}) with
\code{fft_size/2 + 1} spectral coefficients per frame, where
\code{fft_size} is determined by \code{fs} and \code{f0_floor} following
the WORLD formula.

All input media formats supported by \pkg{av} are accepted.
}
\examples{
\dontrun{
# Estimate spectral envelope
trk_cheap_trick("recording.wav")

# Return data without writing file
sp <- trk_cheap_trick("recording.wav", toFile = FALSE)
dim(sp$sp)  # n_frames x 1025 for 16 kHz audio

# Process multiple files
trk_cheap_trick(c("file1.wav", "file2.wav"))
}
}
