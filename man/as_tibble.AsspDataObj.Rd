% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assp_dataobj.R, R/ssff.R
\name{as_tibble.AsspDataObj}
\alias{as_tibble.AsspDataObj}
\title{Convert AsspDataObj to tibble}
\usage{
\method{as_tibble}{AsspDataObj}(
  x,
  field = NULL,
  beginTime = NULL,
  endTime = NULL,
  na.zeros = TRUE,
  convert_units = TRUE
)

\method{as_tibble}{AsspDataObj}(
  x,
  field = NULL,
  beginTime = NULL,
  endTime = NULL,
  na.zeros = TRUE,
  convert_units = TRUE
)
}
\arguments{
\item{x}{An object of class \code{AsspDataObj} (usually created by calling \link{read.AsspDataObj})}

\item{field}{An optional argument indicating either the field name or field index number to extract. If not given (NULL), all fields will be extracted.}

\item{beginTime}{The start time of the portition of the SSFF track that was converted to a tibble. Defaults to zero (0) which means that the extracted portion is expected to start at the beginning of the signal.}

\item{endTime}{The end time of the portition of the SSFF track that was converted to a tibble. Defaults to NULL which means that the extracted portion is expected to end at the end of the signal.}

\item{na.zeros}{Replace all zero (0) values in the track data columns with \code{NA} value? Defaults to \code{TRUE} so that subsequent applications of summary statistics functions do not risk confusing the zero values as actual measurements.}

\item{convert_units}{Logical; if TRUE (default), automatically convert columns with unit labels (e.g., "fo[Hz]") to units objects}

\item{...}{Additional arguments passed to \code{as.data.frame.AsspDataObj()}.}

\item{clean_names}{Logical. If TRUE (default), use underscore notation.}
}
\value{
tibble with clean column names, units (optional), and label attributes.

A \link[tibble:tibble]{tibble::tibble} containing columns \code{times_orig}, \code{times_rel}, \code{times_norm}, followed by one column for each track and track field (separated by '_')
that is, if the user has chosen to convert the output of \code{trk_forest()} to a tibble, then the tibble will have columns \verb{times_orig times_rel times_norm  fm_1  fm_2  fm_3  fm_4  bw_1  bw_2  bw_3  bw_4}.
If the user only wanted the first field (or the "fm" field), then the output tibble will have columns \verb{times_orig times_rel times_norm  fm_1  fm_2  fm_3  fm_4}.
}
\description{
Converts an AsspDataObj to a tibble (tidyverse data frame) with the same
features as \code{as.data.frame.AsspDataObj()}.

This function converts an \code{AsspDataObj} to \link[tibble:as_tibble]{tibble::as_tibble} form.
}
\details{
This is a thin wrapper around \code{as.data.frame.AsspDataObj()} that converts
the result to a tibble. All features (template expansion, clean names,
units) are identical.

Requires the 'tibble' package.

This function redefines the \link[tibble:as_tibble]{tibble::as_tibble} method for \code{AsspDataObj} so that
the output columns works well with \code{reindeer::quantify} and to replicate the output of \link[emuR:get_trackdata]{emuR::get_trackdata}.

Contrary to \link[emuR:get_trackdata]{emuR::get_trackdata} this function assumes that track data that are exactly zero (0) are actually missing measurements,
acknowleging that this is how missing measurements are stored in the SSFF file format. The user should supply an argument \code{na.zeros=FALSE} if
that assumption is risky in the SSFF file that is processed.

If \code{convert_units=TRUE} (the default), the function will automatically detect track names ending with
\verb{\[<unit>\]} (e.g., "fo[Hz]") and convert those columns to units objects using the \code{units} package.
If a unit cannot be parsed, the column will be left as-is with a warning.
}
\examples{
\dontrun{
fms <- wrassp::forest("audio.wav", numFormants = 4, toFile = FALSE)

# Convert to tibble (requires tibble package)
tbl <- as_tibble(fms)

print(tbl)
# # A tibble: 289 × 9
#    frame_time F1_Hz F2_Hz F3_Hz F4_Hz B1_Hz B2_Hz B3_Hz B4_Hz
#         <dbl> <dbl> <dbl> <dbl> <dbl> <dbl> <dbl> <dbl> <dbl>
#  1      0.000  800  1200  2400  3500   80   120   200   250
#  2      0.005  820  1220  2380  3480   85   125   210   260
#  ...
}
}
\seealso{
\code{\link[=as.data.frame.AsspDataObj]{as.data.frame.AsspDataObj()}}
}
