% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_covarep_vq_gci.R
\name{trk_covarep_vq_gci}
\alias{trk_covarep_vq_gci}
\title{GCI-Based Voice Quality Parameters}
\usage{
trk_covarep_vq_gci(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Vector of file paths (WAV, MP3, MP4, etc.) to analyze}

\item{gci_times}{Optional list of GCI times (seconds). If NULL, computed via SEDREAMS.}

\item{beginTime}{Start time in seconds (0 for beginning of file)}

\item{endTime}{End time in seconds (0 for end of file)}

\item{toFile}{Write output to file (TRUE) or return object (FALSE). Default: FALSE}

\item{explicitExt}{Output file extension (default: "vqg")}

\item{outputDirectory}{Output directory (NULL for same as input file)}

\item{verbose}{Show progress messages (default: TRUE)}
}
\value{
If \code{toFile=FALSE} (default): AsspDataObj with 5 tracks (naq, qoq, h1h2, hrf, psp),
each interpolated to 10ms frame grid.

If \code{toFile=TRUE}: invisibly returns vector of output file paths
}
\description{
Computes voice quality parameters per glottal closure instant (GCI),
interpolated to regular 10ms frame grid.
Complements \code{lst_covarep_vq()} (scalar summaries) with full time series.
}
\details{
\strong{Voice Quality Parameters} (computed per GCI, interpolated to 10ms grid):
\itemize{
\item \strong{NAQ} (Normalized Amplitude Quotient): Amplitude shape, related to open quotient
\item \strong{QOQ} (Quasi-Open Quotient): Duration of open phase relative to pitch period
\item \strong{H1H2} (Fundamental to 2nd Harmonic): Spectral tilt measure
\item \strong{HRF} (Harmonic Richness Factor): Overall harmonic content
\item \strong{PSP} (Parabolic Spectral Peak): Spectral peakedness
}

\strong{Method}:
\enumerate{
\item Detect GCIs (SEDREAMS) if not provided
\item Compute glottal flow via IAIF (Inverse Filtering)
\item Extract voice quality measures per GCI
\item Interpolate to regular 10ms frame grid using linear interpolation
}

\strong{Interpretation}:
\itemize{
\item NAQ/QOQ: Phonation mode indicators (creaky, normal, breathy)
\item H1H2/HRF: Spectral characteristics (bright, rich, sparse voice)
\item PSP: Voice quality correlate (higher = smoother spectral envelope)
}
}
\examples{
\dontrun{
# Single file, auto GCI detection
vq <- trk_covarep_vq_gci("speech.wav", toFile = FALSE)

# With pre-computed GCIs
gcis <- lst_covarep_gci_sedreams("speech.wav", f0mean = 100)
gci_times <- gcis$gci_times[[1]]
vq <- trk_covarep_vq_gci("speech.wav", gci_times = gci_times, toFile = FALSE)
}

}
