% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_c_assp_lp_analysis.R
\name{arfana}
\alias{arfana}
\title{Linear Prediction analysis using autocorrelation the Durbin recursion and area function output (Rcpp-optimized)}
\usage{
arfana(listOfFiles = NULL,
  beginTime = 0.0,
  centerTime = FALSE,
  endTime = 0.0,
  windowShift = 5.0,
  windowSize = 20.0,
  effectiveLength = TRUE,
  window = 'BLACKMAN',
  order = 0,
  preemphasis = -0.95,
  toFile = TRUE,
  explicitExt=NULL,
  outputDirectory = NULL,
  assertLossless = NULL,
  logToFile = FALSE,
  keepConverted=FALSE,
  verbose = TRUE)
}
\arguments{
\item{listOfFiles}{Vector of file paths to the audio files to be processed.}

\item{beginTime}{Start time in seconds (default: 0.0).}

\item{centerTime}{Logical; center analysis windows (default: FALSE).}

\item{endTime}{End time in seconds (default: 0.0 for end of file).}

\item{windowShift}{Frame shift in milliseconds (default: 5.0).}

\item{windowSize}{Analysis window size in milliseconds (default: 20.0).}

\item{effectiveLength}{Logical; use effective window length (default: TRUE).}

\item{window}{Window function type (default: 'BLACKMAN').}

\item{order}{Analysis order; 0 for automatic calculation (default: 0).}

\item{preemphasis}{Preemphasis factor (default: -0.95).}

\item{toFile}{Logical; write results to file (TRUE) or return as objects (FALSE).}

\item{explicitExt}{Explicit file extension for output files.}

\item{outputDirectory}{Output directory path.}

\item{assertLossless}{Character vector of lossless formats to assert.}

\item{logToFile}{Logical; enable file logging.}

\item{keepConverted}{Logical; keep converted intermediate files.}

\item{verbose}{Logical; print progress messages (default: TRUE).}
}
\value{
The number of successfully written files (if \code{toFile=TRUE}), or a vector of \code{AsspDataObj} objects (if \code{toFile=FALSE}).
}
\description{
Linear Prediction analysis of \code{listOfFiles} using the
autocorrelation method and the Durbin recursion.
This program calculates the RMS amplitudes of the input
and residual signal in dB and, and area function using algorithms implmented in \emph{libassp} \insertCite{s5h}{superassp}.
Input signals not in a file format natively
supported will be converted before the autocorrelation functions are
computed. The conversion process will display warnings about input files
that are not in known losslessly encoded formats.
}
\details{
The results will be will be written to an SSFF formated file with the base
name of the input file and extension \code{.arf} with tracks \code{RMS[dB]},\code{gain[dB]},and \code{ARF}.

The function is a re-write of the \link[wrassp:rfcana]{wrassp::rfcana} function with area funciton output, but
with media pre-conversion, better checking of preconditions such as the
input file existence, structured logging, and the use of a more modern
framework for user feedback. This version includes Rcpp optimizations
for improved performance on large batches of files.

The native file type of this function is "wav" files (in "pcm_s16le"
format), SUNs "au", NIST, or CSL formats (kay or NSP extension). Input
signal conversion, when needed, is done by
\href{https://ffmpeg.org/libavcodec.html}{libavcodec} and the excellent \link[av:encoding]{av::av_audio_convert}
wrapper function
}
\note{
This function is not considered computationally expensive enough to require caching of
results if applied to many signals. However, if the number of signals it will be applied to
is \emph{very} large, then caching of results may be warranted.
}
\examples{
# get path to audio file
path2wav <- list.files(system.file("samples","sustained", package = "superassp"), pattern = glob2rx("a1.wav"), full.names = TRUE)

# perform linear prediction analysis
res <- arfana(path2wav, toFile=FALSE)

# plot reflection coefficients
matplot(seq(0,numRecs.AsspDataObj(res) - 1) / rate.AsspDataObj(res) +
          attr(res, 'startTime'),
        res$ARC,
        type='l',
        xlab='time (s)',
        ylab='Area function')

}
\references{
\insertAllCited{}
}
\seealso{
\link[wrassp:rfcana]{wrassp::rfcana}

\link{AsspWindowTypes}

\link[av:encoding]{av::av_audio_convert}
}
\author{
Raphael Winkelmann

Lasse Bombien

Fredrik Nylén
}
