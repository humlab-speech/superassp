% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensure_superassp.R
\name{ensure_superassp}
\alias{ensure_superassp}
\title{Ensure All superassp Python Dependencies Are Installed}
\usage{
ensure_superassp(
  envname = "r-speech",
  method = c("auto", "conda", "virtualenv"),
  install_numba = TRUE,
  install_cython = TRUE,
  force = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{envname}{Name of Python environment to create or use. Default: \code{"r-speech"}}

\item{method}{Installation method: \describe{
\item{\code{"auto"}}{Try conda first, then virtualenv, then system Python (default)}
\item{\code{"conda"}}{Use conda environment manager only}
\item{\code{"virtualenv"}}{Use Python virtualenv only}
}}

\item{install_numba}{Logical. Install Numba JIT compiler for optimized functions?
Default: TRUE. Provides 10-20x speedups with no compilation required.}

\item{install_cython}{Logical. Install Cython for compiled extensions?
Default: TRUE. Provides 15-25x additional speedups (requires C compiler).}

\item{force}{Logical. Reinstall all packages even if already present?
Default: FALSE.}

\item{verbose}{Logical. Print installation progress and summary?
Default: TRUE.}
}
\value{
Invisibly returns a named logical vector with pass/fail status for each
package group. Names include: \code{core}, \code{audio}, \code{small}, \code{pytorch}, \code{tensorflow},
\code{phonet}, \code{numba}, \code{cython}, and one per local module (e.g., \code{voice_analysis},
\code{brouhaha}, \code{voxit}).
}
\description{
Installs all Python module dependencies for superassp in one unified environment.
This is a best-effort installer that attempts to set up ~12 independent Python
module groups in a single shared environment. Core numeric packages (numpy, scipy,
etc.) must install successfully; optional packages (PyTorch, TensorFlow, etc.)
are installed on a best-effort basis.
}
\details{
superassp includes ~12 independent Python module groups:
\itemize{
\item \strong{Core} (hard requirement): numpy, scipy, soundfile, pandas, scikit-learn
\item \strong{Audio}: librosa, SPTK, resampy, spectral features
\item \strong{Voice Quality}: brouhaha-VAD, COVAREP, voxit, voice_analysis (VAT)
\item \strong{Pitch/Formants}: CREPE, pYIN, YIN, deepformants
\item \strong{PyTorch stack}: torch, torchaudio, pyannote.audio (brouhaha)
\item \strong{TensorFlow stack}: tensorflow, phonet
\item \strong{Local modules}: bundled editable installations from inst/python/
}

\code{ensure_superassp()} attempts to install all groups, aborting only if core packages
fail. Optional packages (PyTorch, TensorFlow, local modules) may fail gracefully without
stopping the process.
}
\section{Environment Selection}{


When \code{method = "auto"}:
\enumerate{
\item Check if conda is available → use conda
\item Check if virtualenv is available → use virtualenv
\item Fall back to system Python
}

Explicitly setting \code{method = "conda"} or \code{method = "virtualenv"} skips detection.
}

\section{What Gets Installed}{


\strong{Phase A — Core (hard requirement, aborts on failure)}:
numpy, scipy, soundfile, pandas, scikit-learn, joblib

\strong{Phase B — Audio/Signal (soft failure)}:
librosa, SPTK, onnxruntime (swift-f0), pywt, python_speech_features,
resampy, tqdm, six

\strong{Phase C — Numba JIT (optional accelerator)}:
numba (if \code{install_numba = TRUE})

\strong{Phase D — Cython (optional, requires C compiler)}:
cython (if \code{install_cython = TRUE} and C compiler detected)

\strong{Phase E — Small/Specialized}:
lempel_ziv_complexity, nolds, EMD-signal, pyworld, matplotlib

\strong{Phase F — PyTorch Stack (soft failure)}:
torch~=2.8.0, torchaudio~=2.8.0, pyannote.audio>=3.3.2,<4.0.0 (for brouhaha-VAD)

\strong{Phase G — TensorFlow Stack (soft failure)}:
tensorflow, tf-keras, phonet (GitHub)

\strong{Phase H — Local Module Editable Installs}:
voice_analysis_python, covarep_python, voxit, brouhaha-vad, ftrack_tvwlp,
legacy_STRAIGHT (from inst/python/)
}

\section{Conflict Handling}{


\strong{numpy version}: Pinned to \verb{>=1.24.0,<2.0} to satisfy legacy_STRAIGHT's upper
bound while meeting all other modules' lower bounds (>=1.19-1.24).

\strong{PyTorch vs TensorFlow}: Both can be installed in the same environment with
careful version selection. If you encounter conflicts, consider:
\itemize{
\item Running \code{ensure_superassp()} in a fresh environment with only one framework
\item Using separate environments for GPU-intensive work
\item Checking NVIDIA CUDA compatibility documentation
}
}

\section{Error Behavior}{


The function aborts (via \code{rlang::abort()}) if:
\itemize{
\item \code{reticulate} package is not available
\item Python >= 3.8 cannot be found
\item Core numeric packages (numpy, scipy, etc.) fail to install
}

The function continues with warnings if:
\itemize{
\item Optional packages (PyTorch, TensorFlow, etc.) fail
\item Cython compilation fails (module still works without it)
\item Local module editable installs fail
}
}

\examples{
\dontrun{
# Install everything in a new "r-superassp" conda environment
ensure_superassp(method = "conda", verbose = TRUE)

# Check status
result <- ensure_superassp(verbose = FALSE)
print(result)
#> core audio small pytorch tensorflow numba cython brouhaha voxit
#> TRUE  TRUE  TRUE    TRUE       TRUE TRUE   TRUE       TRUE   TRUE

# Reinstall all packages (useful after updating Python)
ensure_superassp(force = TRUE)

# Minimal install: core + audio only (skip optional Numba/Cython)
ensure_superassp(install_numba = FALSE, install_cython = FALSE)
}

}
\seealso{
Environment management: \code{\link[reticulate]{conda_create}},
\code{\link[reticulate]{virtualenv_create}}.
}
