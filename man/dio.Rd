% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python_ssff.R, R/superassp_sptk_pitch.R
\name{dio}
\alias{dio}
\title{Compute f0 using the Harvest algorithm}
\usage{
dio(
  listOfFiles = NULL,
  beginTime = 0,
  endTime = 0,
  windowShift = 10,
  minF = 60,
  maxF = 400,
  voicing_threshold = 0.85,
  toFile = TRUE,
  explicitExt = "f0",
  outputDirectory = NULL,
  verbose = TRUE
)

dio(
  listOfFiles = NULL,
  beginTime = 0,
  endTime = 0,
  windowShift = 10,
  minF = 60,
  maxF = 400,
  voicing_threshold = 0.85,
  toFile = TRUE,
  explicitExt = "f0",
  outputDirectory = NULL,
  verbose = TRUE
)
}
\arguments{
\item{listOfFiles}{Vector of file paths to process}

\item{beginTime}{Start time in seconds (default: 0.0)}

\item{endTime}{End time in seconds (default: 0.0 = end of file)}

\item{windowShift}{Frame shift in milliseconds (default: 10.0)}

\item{minF}{Minimum F0 in Hz (default: 60.0)}

\item{maxF}{Maximum F0 in Hz (default: 400.0)}

\item{voicing_threshold}{Voicing threshold (default: 0.85)}

\item{toFile}{Write results to file (default: TRUE)}

\item{explicitExt}{Output file extension (default: "f0")}

\item{outputDirectory}{Output directory (default: NULL = same as input)}

\item{verbose}{Show progress messages (default: TRUE)}

\item{voiced.voiceless.threshold}{Threshold for voiced/unvoiced decision. Can
be any value >= 0, but 0.02 to 0.2 is a reasonable range. Lower values will
cause more frames to be considered unvoiced (in the extreme case of
\code{threshold=0}, almost all frames will be unvoiced).}
}
\value{
An SSFF track object containing two tracks (f0 and corr) that are
either returned (toFile == FALSE) or stored on disk.

If toFile=TRUE, returns the number of successfully processed files.
If toFile=FALSE, returns AsspDataObj or list of AsspDataObj objects.
}
\description{
The DIO algorithm \insertCite{morise2010rapid}{superassp} was developed for
the WORLD vocoder
\insertCite{MORISE.2016.10.1587/transinf.2015edp7457}{superassp} and aims
provide a fast estimate of the f0 contour.

Extract F0 using the DIO algorithm from the WORLD vocoder (via SPTK).
DIO is designed for high-quality pitch extraction for speech synthesis applications.
}
\examples{
\dontrun{
# Extract F0 using DIO
dio("recording.wav")

# Process with custom F0 range
dio("speech.wav", minF = 80, maxF = 350)
}
}
\references{
\insertAllCited{}
}
