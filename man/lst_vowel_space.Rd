% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_vowel_space.R
\name{lst_vowel_space}
\alias{lst_vowel_space}
\title{Vowel space analysis (F1×F2 area ratio)}
\usage{
lst_vowel_space(
  formant_data,
  gender = 1,
  mode = "triangle",
  scaling = FALSE,
  plot_formants = FALSE
)
}
\arguments{
\item{formant_data}{Data frame or matrix with columns F1, F2 (or first two columns).
F1 and F2 should be in Hz.}

\item{gender}{Speaker gender for reference vowel selection:
0 = female, 1 = male (default), 2 = child}

\item{mode}{Vowel space computation mode:
"triangle" = use 3 corner vowels (default, more robust)
"polygon" = use 4 vowels (may be less stable)}

\item{scaling}{Apply Bark frequency scaling (default: FALSE)}

\item{plot_formants}{Show scatter plot of formant points (default: FALSE)}
}
\value{
List with elements:
\itemize{
\item \code{vowel_space_ratio}: Numeric, ratio of actual to reference vowel space area (0–2+ range)
\item \code{centroids}: Matrix of estimated vowel formant centroids from k-means clustering
\item \code{n_frames}: Number of frames used in computation
}
}
\description{
Computes vowel space area from formant data using k-means clustering and
reference vowel comparison. The vowel space ratio is computed as the ratio
of the actual vowel space convex hull area to a reference vowel space area
for the same gender.
}
\details{
\strong{Algorithm}:
\enumerate{
\item Filter formant data by gender-specific frequency ranges to identify candidate vowels
\item Run k-means clustering using reference vowel centroids as initialization
\item Match estimated centroids to reference vowels using Mahalanobis distance
\item Compute convex hull of matched vowel points (actual vowel space)
\item Compute convex hull of reference vowels
\item Ratio = actual area / reference area
}

\strong{Interpretation}:
\itemize{
\item Ratio = 1.0: perfect agreement with reference vowel space
\item Ratio < 1.0: speaker has reduced vowel space (dysarthria, age, etc.)
\item Ratio > 1.0: speaker has expanded vowel space (hyperarticulation, singing)
\item NaN/error: insufficient vowel tokens (<1000 frames) in valid range
}

\strong{Reference vowel sets}:
\itemize{
\item Triangle: /i/, /a/, /u/ (corner vowels)
\item Polygon: /i/, /ε/, /a/, /u/ (4-vowel system)
}

\strong{Minimum frame requirement}: 1000 frames must be within gender-specific frequency bounds,
or function returns 0 ratio (insufficient data).
}
\examples{
\dontrun{
# Compute vowel space from file batch
files <- c("speaker1.wav", "speaker2.wav")
formants <- trk_formant(files, toFile = FALSE)

# Extract F1-F2 and compute vowel space
formant_df <- data.frame(F1 = formants$fm1, F2 = formants$fm2)
vs <- lst_vowel_space(formant_df, gender = 1)
cat("Vowel space ratio:", vs$vowel_space_ratio, "\n")

# For female speaker
vs_female <- lst_vowel_space(formant_df, gender = 0)

# Using Bark scaling
vs_bark <- lst_vowel_space(formant_df, gender = 1, scaling = TRUE)
}

}
