% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssff_python_straight_f0.R
\name{trk_straight_f0}
\alias{trk_straight_f0}
\title{STRAIGHT F0 Extraction (Legacy Vocoder)}
\usage{
trk_straight_f0(listOfFiles, ...)
}
\arguments{
\item{listOfFiles}{Character vector of audio file paths, or AVAudio object}

\item{...}{Additional arguments (for S7 dispatch compatibility)}

\item{beginTime}{Numeric; start time in seconds (default: 0, start of file)}

\item{endTime}{Numeric; end time in seconds (default: 0, end of file)}

\item{f0_floor}{Numeric; minimum F0 in Hz (default: 40)}

\item{f0_ceil}{Numeric; maximum F0 in Hz (default: 800)}

\item{frame_shift}{Numeric; frame shift in milliseconds (default: 1.0)}

\item{toFile}{Logical; write output to SSFF file (default: FALSE for in-memory)}

\item{explicitExt}{Character; file extension for output (default: "strf0")}

\item{outputDirectory}{Character; output directory (default: NULL, same as input)}

\item{verbose}{Logical; print progress messages (default: TRUE)}
}
\value{
If \code{toFile = FALSE}: AsspDataObj (single file) or list of AsspDataObj
(multiple files) with tracks: \code{f0} (Hz), \code{vuv} (0/1 voice/unvoiced),
\code{if_score}, \code{ac_score}.
If \code{toFile = TRUE}: Character vector of output file paths (returned invisibly).
}
\description{
Extracts fundamental frequency (F0) using the legacy STRAIGHT multicue
algorithm. Provides >99\% accuracy compared to the original MATLAB
implementation.
}
\details{
The STRAIGHT F0 extraction algorithm uses a multi-cue approach combining:
\itemize{
\item Instantaneous Frequency (IF) analysis
\item Autocorrelation (AC) analysis
\item Template matching and fusion
\item Fixed-point refinement
}

\strong{Performance}:
\itemize{
\item Without Numba: ~0.81s for 0.79s audio (1.02x RT)
\item With Numba: ~0.68s for 0.79s audio (0.86x RT, 20\% faster)
\item Install Numba: \code{install_legacy_straight(install_numba = TRUE)}
}

\strong{Accuracy}: >99\% frame-level agreement with MATLAB STRAIGHT

\strong{Audio Loading}: Uses \code{av} package for universal media support (WAV, MP3,
MP4, etc.). Time windowing and format conversion handled automatically.
}
\examples{
\dontrun{
# Check availability
if (!straight_available()) {
  install_legacy_straight()
}

# Single file F0 extraction
wav_file <- system.file("samples", "sustained", "a1.wav", package = "superassp")
f0_data <- trk_straight_f0(wav_file, toFile = FALSE)

# Access F0 track
plot(f0_data$f0[,1], type = "l", ylab = "F0 (Hz)")

# Batch processing with custom F0 range
files <- list.files("audio/", pattern = "\\\\.wav$", full.names = TRUE)
f0_results <- trk_straight_f0(files, f0_floor = 80, f0_ceil = 400, toFile = FALSE)

# Write to SSFF files
trk_straight_f0(wav_file, toFile = TRUE, outputDirectory = "output/")

# With AVAudio object (in-memory)
audio <- read_avaudio(wav_file, sample_rate = 22050)
f0_data <- trk_straight_f0(audio, toFile = FALSE)
}

}
\references{
\insertRef{Kawahara.1999.SpeechCommunication}{superassp}
}
\seealso{
\code{\link{install_legacy_straight}}, \code{\link{straight_available}},
\code{\link{trk_straight_spec}}, \code{\link{trk_rapt}}, \code{\link{trk_swipe}}
}
