# Detect platform for ONNX Runtime download

Detect platform for ONNX Runtime download

## Usage

``` r
.ort_detect_platform(gpu = FALSE)
```

## Arguments

- gpu:

  Logical. Request GPU variant.

## Value

List with os, arch, slug.
