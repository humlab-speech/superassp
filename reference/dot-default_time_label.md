# Default x-axis label for a track table

Default x-axis label for a track table

## Usage

``` r
.default_time_label(data)
```

## Arguments

- data:

  data.frame or NULL. Wide track table.

## Value

"Time \[s\]" for a table with a `frame_time` column, a waiver otherwise.
