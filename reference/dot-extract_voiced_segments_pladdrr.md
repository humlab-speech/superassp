# Extract and concatenate voiced segments from continuous speech

Extract and concatenate voiced segments from continuous speech

## Usage

``` r
.extract_voiced_segments_pladdrr(sound, version = "v3.01")
```
