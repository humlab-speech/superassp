# Cleanup converted input media files

Cleanup converted input media files

Cleanup converted input media files

## Usage

``` r
cleanupConvertedInputMediaFiles(toClear, keepConverted, verbose)

cleanupConvertedInputMediaFiles(toClear, keepConverted, verbose)
```

## Arguments

- toClear:

  Character vector of files to remove

- keepConverted:

  Logical whether to keep converted files

- verbose:

  Logical whether to show progress messages
