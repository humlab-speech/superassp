# Prepare a file path for signal processing functions (Rcpp-optimized)

Normalise a list of filenames so that they can be passed to a signal
processing function

Normalise a list of filenames so that they can be passed to a signal
processing function

## Usage

``` r
prepareFiles(listOfFiles)

prepareFiles(listOfFiles)
```

## Arguments

- listOfFiles:

  The list of file names to process

## Value

A normalised list of filenames

A normalised list of filenames

## Author

Raphael Winkelmann
