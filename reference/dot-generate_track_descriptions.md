# Generate track descriptions for data frame

Creates a named list mapping column names to full descriptive labels.

## Usage

``` r
.generate_track_descriptions(col_names)
```

## Arguments

- col_names:

  Character vector. Column names.

## Value

Named list mapping column names to descriptions.
