#!/bin/sh
# Remove the artifacts ./configure.win leaves behind: the generated
# src/Makevars and the openSMILE CMake build tree.
PKG_ROOT="$(cd "$(dirname "$0")" && pwd)"
rm -f "$PKG_ROOT/src/Makevars"
rm -rf "$PKG_ROOT/src/opensmile/.build_r"
exit 0
